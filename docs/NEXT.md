# 다음에 할 일

작성 2026-09-07. 다른 컴퓨터에서 이어받을 때 이 파일만 읽으면 된다.
`main`에 있다(브랜치 `add-three-rfps-and-v5-dataset`과 같은 커밋).

---

## ⚠️ 먼저 — 모든 명령에 `RFP_DATASET_VERSION=v5`

`DEFAULT_DATASET_KEY`가 아직 `v4`다. **빠뜨리면 조용히 v4 라벨로 학습한다.**
TF-IDF는 다시 돌리면 그만이지만 **GPU 파인튜닝은 예산을 그대로 날린다.**

기본값을 v5로 바꾸려 했으나 동결 데이터셋 감사 테스트 22개가 깨져서 되돌렸다
(`test_label_dataset.py`, `test_requirement_taxonomy.py` 등이 기본값에 기대고 있다).
옮기려면 그 테스트들이 v4를 명시적으로 요구하도록 먼저 고쳐야 한다. 급하지 않다.

---

## 0. 문서 맥락 재판정 — **끝남, 기각** (2026-09-08 01:30, decisions-09)

Solar로 돌렸다. 공통 82건에서 정답 일치 49 → 54, 짝 비교 8:3(p 0.23), **경계 혼동 7건은 3:3으로
불변.** 맥락 카드는 채택하지 않는다. Claude 팔은 키 복구 후 `--provider claude --execute`로
돌릴 수 있지만 우선순위 없음. 경계 혼동에 대한 처방은 "2분류 운영 + 사람 판정"으로 확정.

## 1. 파인튜닝 v5 — **끝남** (2026-09-07 17:10, decisions-09)

`wc + ftB + ftL` 다수결 fold 평균 **0.666~0.683**(seed 9쌍, 평균 0.6745). 기준선 0.6395
대비 전부 측정 하한 초과. 파인튜닝 단독은 large 평균 0.652(+0.012)로 하한 아래.
`reports/current/v5/finetune_results.md`. 이 축에서 더 돌릴 것은 없다.

## 3. 문서 +3개 ← **다음은 여기** (GPU 불필요, 라벨링 약 $4, 실행 전 사용자 확인)

## 3.5 라벨 규칙 v7 — **점수 목적으로는 안 함, 논문 목적으로 후순위** (2026-09-08 10:40)

검토판 C 탭 27건 완료. Claude "모델이 맞다" 27건 중 사용자도 모델 편 15건, v5 편 11건, 보류 1.
v5가 실제로 틀린 건은 127건 중 10~15건 → 재라벨링 이득 0.01 안팎(하한 아래). 규칙 후보 셋과
사용자 판단 기준 여섯은 decisions-09 10:40. 문서 확충·3회 다수결 뒤 여력 있으면 논문 근거용으로.

## 4. 보조 헤드 — **이미 해봤고 효과가 없다** (순위 내림)

`aux-heads` 브랜치에 **다중 헤드가 이미 구현돼 있고 v4에서 5회 돌렸다.**
`scripts/modeling/finetune.py`의 `MultiHeadModel`, `AUX_HEADS`, `--aux`, `--aux-weight`.
보조 축은 `blockers`(다중 라벨 BCE) + `cost_basis` + `build_difficulty` +
`domain_dependency`이고, 인코더의 `[CLS]` 표현을 공유한다.

결과가 신통치 않다. `reports/current/v4/finetune_results.md`(aux-heads 브랜치) 기준이며,
결정 기록은 `docs/history/decisions-09.md` 2026-09-07 09:30에 있다.

| 설정 | +aux | 원본 | 차이 | 경계 혼동 |
|---|---:|---:|---:|---:|
| base seed 42 | 0.588 | 0.573 | +0.015 | 118 (원본 111) |
| base seed 7 | 0.594 | 0.609 | **-0.015** | 105 (104) |
| base seed 13 | 0.589 | 0.597 | -0.008 | 112 (104) |
| large seed 42 | 0.573 | 0.617 | **-0.044** | 113 (107) |
| large seed 7 | 0.604 | 0.595 | +0.009 | 105 (98) |

**부호가 오가고 평균 -0.009다.** 전부 측정 하한(0.016) 근처이거나 아래이고, 경계 혼동은
오히려 늘었다. 보조 축을 주는 것으로는 안 된다는 것이 v4에서 이미 나왔다.

남은 변형은 보조 헤드의 타깃을 **v5/v6 소프트 분포**로 바꾸는 것이다
(Fornaciari et al., NAACL 2021 `2021.naacl-main.204` — 주 과제는 하드 라벨 유지,
보조 헤드가 분포를 KL로 예측). 앞서 한 번 돌린 -0.047은 하드 라벨을 소프트 분포로
**대체**한 것이라 그 논문의 방법이 아니고 판정도 아니다.

**그래도 우선순위는 낮다.** 같은 구조에서 보조 축 넷이 모두 실패했고, 타깃만 바꿔
0.016을 넘길 근거가 약하다. 3번을 먼저 하고, 여력이 남으면 본다.

구현하려면 `aux-heads`의 `finetune.py`에서 `MultiHeadModel` 계열을 가져와야 한다.
**단, 두 브랜치의 `finetune.py`가 갈려 있어 자동 병합이 안 된다**(aux-heads에는
`run_id`가 없고, main에는 `MultiHeadModel`이 없다). 통째 병합 말고 필요한 부분만
옮긴다.


학습곡선 `macroF1 = 0.0896·ln(학습문서수) + 0.4241`(가중 R² 0.969).
11문서 예측 0.6390 대 실측 0.6395로 검증됐다.

| 총 문서 | 추가 | 예상 | 현재 대비 |
|---:|---:|---:|---:|
| 16 | +3 | 0.661 | +0.021 |
| 19 | +6 | 0.678 | +0.039 |
| 24 | +11 | 0.701 | +0.062 |

**최소 3개를 한 번에 넣어야 한다.** 1~2개는 이득(0.007~0.014)이 측정 하한 아래라
올랐는지 아닌지도 모른다.

라벨링 배치가 421건에 $3.83이었으니 문서당 약 $1.3. 문서는 나라장터에서 구한다.
**실행 전 사용자 확인 필요**(유료 API).

절차는 `docs/history/decisions-07.md` 2026-09-07 03:04 항목에 있다. `.hwp`·`.xlsx`
모두 `scripts/data/hwp_tables.py`·`xlsx_tables.py`로 원본에서 직접 표를 읽는다.

---

## 5. 데이콘 대회 연계 (별도 저장소, 마감 2026-09-29)

나라장터 입찰공고 법령 위반 24항목 판정. 규칙이 학습된 분류기를 금지하므로 **모델이 아니라
라벨링 파이프라인·앵커 설계·인용 후처리·측정 하한 논리**를 가져간다. 근거와 목록은
`docs/history/decisions-09.md` 2026-09-07 21:10.

1. 데이터 받아 `항목표.json`·`dev.jsonl` 확인 → 24항목을 규칙형(meta) / LLM형 / 부재형으로 분류
2. meta 규칙 먼저 구현, dev 200으로 항목별 확인
3. `run_claude_batch.py`를 24항목 스키마로 옮겨 1~2천 건 자가 라벨링, 3회 일치로 사례집 필터
4. bge-m3 군집 층화로 사례집 구성 → gemma 프롬프트 RAG few-shot
5. 근거 문구 부분문자열 검증·재탐색 후처리 → 제출

일정: 라벨링·사례집 1주, 규칙·RAG 1주, 후처리·제출 1주. 하루 1회 제출이라 dev로 먼저 거른다.

## 하지 않을 것 (측정 하한 아래이거나 이미 기각)

| | 결과 |
|---|---|
| XGBoost·트리 계열 | 0.5946, 용량 올리면 0.5610. 앙상블에 넣어도 안 오름 |
| 마스킹 R1/R2/R3 | v5에서 -0.004. v4의 +0.018은 애초에 잡음(우연일 확률 17.2%) |
| 안정 라벨만 학습 | -0.087. 소수 클래스가 골라서 지워짐 |
| 소프트 프롬프트 | 100억 파라미터 규모에서나 유효. roberta-large는 3.4억 |
| 피처 변형류 | 기대 효과 0.005~0.015로 측정 하한 아래 |
| 임베딩 군집 확산도 특징 | v5에서 -0.005~-0.018, 우세 최대 6/13. 임베딩 축은 닫음(decisions-09 18:40) |

**새 실험을 고를 때 기준: 기대 효과가 0.016 미만이면 돌리지 않는다.**
결과가 어느 쪽으로 나와도 해석할 수 없다.

## 미뤄둔 것 (우선순위 없음, WORKLOG에서 옮김)

- `무작위 few-shot` 실험 (§8.2) — 무작위와 층화가 비슷하면 앵커 검색기를 유지할 이유가 사라진다
- Chunk 1 배치 재실행 (약 1,350원) — 실행 경로 통일 + 동기/배치 비교 확보 (issues/004)
- `산출정보` 추출 — 넣기로 한다면 100건 파일럿(약 800원)으로 먼저 판단 (issues/007)
- `data/processed/`가 gitignore라 `requirements_v0.4.0.jsonl`이 저장소에 없다. 다른
  컴퓨터에서는 `python -m scripts.data.build_dataset --strict`로 먼저 만든다
  (decisions-06 2026-09-06 21:30 "남은 판단")

---

## 알아둘 것

- **경계 혼동은 어떤 개입으로도 안 움직인다.** v5에서 127건(오답의 32%)이고,
  v4에서 앙상블이 오답을 39건 줄이는 동안 경계는 98 → 97로 한 건 줄었다.
  점수가 오르더라도 이득은 경계 밖에서 나온다. 자세한 것은
  `notebooks/20_boundary_cases.ipynb`. 왜 안 움직이는지는 `21_cluster_diagnostics.ipynb` —
  경계 혼동은 뜻이 비슷한 조항이 다른 RFP에서 다른 라벨을 받은 자리다(이웃 일치 26%, 정답 건 79%).
- 테스트는 **292 통과 / 3 실패**가 정상이다. 실패 3건은 전부 기존 문제 —
  `fasttext-wheel`이 py3.13 빌드가 없고, `05_finetune_analysis.ipynb`가 셀 24개에
  번호가 `05_`로 중복이라 노트북 테스트 2건을 깬다. 그대로 두기로 했다.
- 실행 조건은 `run_id`가 자동으로 박는다(`roberta-large.s42.len512.v5` 꼴).
  결과 파일을 열면 어느 데이터셋으로 돌렸는지 확인할 수 있다.
