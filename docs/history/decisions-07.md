## 2026-09-06 18:08

- 결정: 9월 4일 제출 후 회수하지 않았던 v6c 재점검 배치 2건(각 289건)을 내려받아
  기록에 넣고, **유형 기반 앵커 인출을 채택하지 않는다.** v6c는 v6b의 회귀를 크게
  고쳤음을 확인했으나, v5와 v6c 중 어느 쪽을 쓸지는 일치율로 판정할 문제가 아니므로
  열어 둔다. 데이터셋·앵커 풀은 그대로 둔다.

- 이유: 배치 결과는 29일간만 보관된다. `recheck_union_v6c_rep1`(289/289 성공)과
  `rep2`(288 성공, 1건은 `reasoning` 300자 초과)를 회수해 `results.jsonl`로 남겼다.
  `batch_info.json`은 여전히 `status: in_progress`로 박제돼 있다 — 제출 직후 값이라
  API에 묻지 않으면 완료를 알 수 없다. `batch_chunk2`에 이어 **두 번째로 같은 이유로
  결과를 놓칠 뻔했다.**

  v6c는 v6b의 `견적 → 통상` 붕괴를 실제로 고쳤다. 동결이 `견적반영`인 재점검 89건에서
  견적 유지가 v6b 43건(48.3%)에서 v6c 58~60건(65~67%)으로 올랐고, `고급·전문인력`
  원가는 24건에서 46~47건으로 돌아왔다. `계약·질의검토` 축소라는 원래 목적도
  동결 34.9% 대비 16~17%로 유지된다. `단가가 붙는 항목일 때만` 한 줄이 원인이라는
  진단이 맞았다는 뜻이다. v6c 자기일치는 252/288 = 87.5%, Wilson 95% CI
  (0.832–0.908)로 결정 23의 기준선 92.5%와 겹친다.

- 관측 1 — **v6c에는 결정 21의 고정 규칙과 어긋나는 라벨이 8~9건(2.8~3.1%) 있다.**
  `cost_basis`가 `없음`이 아닌데 `primary_action`이 `통상수용`인 건들이다. 동결
  데이터셋은 결정 30의 규칙 보정이 적용돼 같은 불일치가 0건이므로, 보정 없이 두 쪽을
  비교하면 v6c에 불리하게 기운다. 보정을 적용하면 견적 유지가 60~62건(67~70%)으로,
  통상으로 내려간 건이 29건에서 21건으로 줄어든다. **v6 계열 파이프라인에 규칙 보정이
  빠져 있다.** 프롬프트를 바꾸기 전에 이것부터 맞춰야 두 라벨셋이 같은 기준이 된다.

- 관측 2 — **층화 인출이 유형별 사전확률을 지운다.** `get_stratified_top_k_anchors`는
  세 라벨에서 각각 1건씩 뽑아 항상 1:1:1 균형 세트를 만든다. 그런데 전수 1,024건에서
  유형별 라벨 분포는 균형과 거리가 멀다 — `시스템장비구성`은 통상수용 1.3%,
  `프로젝트관리`는 76.2%다. 양쪽에 같은 균형 세트를 보여주면 그 눈금이 프롬프트에
  실리지 않는다. 008이 잰 인출 유사도 중앙값 0.090을 함께 놓으면, 현재 앵커는
  **"이런 라벨이 있다"는 목록**을 가르치지 **"이런 종류는 보통 이렇다"는 눈금**을
  가르치지 않는다. v6b·v6c가 zero-shot(`--retrieval none`)으로 간 것은 그 눈금을
  프롬프트 규칙으로 대체하려는 시도였다.

- 관측 3 — **유형 눈금은 문서를 넘어가지 않는다.** 2026-08-25 15:37이 유형 one-hot을
  기각하며 적은 "문서별 라벨 분포와 얽혀 불안정한 힌트"를 앵커 쪽에서 재확인했다.
  유형별 `통상수용` 비율의 문서 간 편차는 `기능` 22.7%p(6.7~70.5%), `보안` 30.7%p,
  `테스트` 32.0%p, `컨설팅` 41.0%p다. LODO로 9문서에서 만든 유형 눈금이 빠진 문서를
  맞히는 오차는 18.9%p로, 유형을 무시한 전체 평균(26.0%p)보다 낫지만 이기는 비율이
  62%에 그친다. **전수에서 뽑은 유형별 분포표는 대부분의 유형에서 pooled 착시다.**
  그래서 유형 기반 앵커 인출을 채택하지 않는다. 분류기 특징으로 떨어진 이유와 같다.

  예외는 `시스템장비구성`이다. 6개 문서 전부에서 통상수용이 0~3.4%, 편차 1.3%p로
  문서를 타지 않는다. 결정 30이 장비·인프라 원가를 견적반영으로 고정한 규칙과 같은
  방향이다.

- 관측 4 — **문서 간 문자 유사도로는 "다른 RFP에도 있는 조항"을 못 찾는다.**
  `duplication.cross_document_similarity`로 재보니 동결 견적반영 89건 중 v6c가 견적을
  유지한 65건의 중앙값이 0.159, 통상으로 내린 24건이 0.178로 갈리지 않았다. 개별 건은
  방향이 거꾸로다 — 10개 문서 전부에 있는 `AI 활용 교육`이 0.184인데 한 문서에만 있는
  `매뉴얼 동영상 제작`이 0.396이다. 보편적인 조항일수록 기관마다 문구가 다르기
  때문이다. 006이 잰 문구 반복과 여기서 필요한 **조항 종류의 보편성**은 다른 것이다.

- 관측 5 — 사람 검토로 갈린 건을 읽을 때 **재출현 문서 수가 실무 기준과 맞는다.**
  `헬프데스크` 4/10 문서, `사용자 교육 운영` 10/10, `하자보수·유지관리` 10/10,
  `인수인계` 8/10인 반면 `매뉴얼 동영상 제작`은 1/10(koen)이다. v6c가 통상수용으로
  내린 건 중 교육·헬프데스크는 실무자 판단으로도 기준 수행 범위가 맞고, 동영상 제작만
  사업 고유 항목으로 남았다. 정규식 집계라 정밀하지 않지만, **필요한 신호의 단위가
  문구도 12종 유형도 아닌 "조항의 종류"** 라는 점은 분명하다.

- 논문 메모:
  - 프롬프트 한 줄이 라벨 분포를 얼마나 움직이는지 v6b가 보여준다. `단가가 붙는 항목일
    때만`이라는 조건절 하나가 `견적 → 통상` 127건, `고급·전문인력` 원가 98건을
    지웠다. 규칙 기반 프롬프트 설계의 취약점 사례로 쓴다.
  - 앵커의 목적을 "유사 사례 제시"가 아니라 **"눈금 전달"** 로 다시 정의해야 한다.
    008의 인출 유사도 0.090은 앵커가 유사 사례가 아니었음을 이미 말하고 있었고,
    층화 균형은 눈금 쪽도 막고 있었다. 두 목적이 섞여 있어 어느 쪽으로도 작동하지
    않은 것이 현재 설계의 문제다.
  - 유형(12종)은 사전확률을 담기에 너무 굵고, 문자 유사도는 너무 잘다. 둘 다 실패한
    관측을 기록으로 남긴다. 조항 종류 단위의 군집은 아직 시험하지 않았다.
  - `label_hints.py`의 `[드문 표현]`은 문서빈도 3% 이하만 잡으므로 **드문 것만** 알린다.
    관행 조항이 흔하다는 사실은 어떤 힌트로도 전달되지 않는다. 힌트 모듈 주석이 스스로
    적은 한계와 같다.

- 산출물: `reports/current/claude_batches/recheck_union_v6c_rep{1,2}/results.jsonl`
  (aux-heads 브랜치 경로), `scripts/evaluation/run_agreement.py`,
  `notebooks/17_rerun_agreement.ipynb`.

## 2026-09-06 19:24

- 결정: 결정 구조 변형 세 가지(OvR·OvO·캐스케이드)를 통제 비교하고 **셋 다 채택하지
  않는다.** 기준선 `word+char + balanced Logistic`의 multinomial 구성을 유지한다.
  아울러 `finetune_ensemble.load_members`의 멤버 태그 충돌을 고쳤다.

- 이유: 오답의 3분의 1이 `견적반영`↔`계약·질의검토` 경계에서 나오는데, 파인튜닝·용량·
  입력 변형·앙상블이 모두 그 숫자를 못 움직였다(2026-09-02 22:06). 남은 의심은 세 라벨을
  한 분류기로 가르는 **결정 구조 자체**였다. OvO는 `견적 대 계약`만 보는 분류기를 실제로
  만들고, 캐스케이드는 1단계에서 `통상수용`을 아예 치워 2단계가 경계만 보게 한다. 구조가
  원인이라면 여기서 경계 혼동이 줄어야 한다.

  word+char 기준 fold 평균 macro F1은 multinomial 0.6144, OvR 0.6137, OvO 0.6130,
  캐스케이드 0.5957이고 경계 혼동은 각각 98 / 97 / 102 / 106이다. char 입력도 같은
  방향이다(0.6077 / 0.6013 / 0.6040 / 0.5865). **경계 혼동이 87~106 사이에서 움직이지
  않는다.** 검토 recall은 오히려 0.568에서 0.524~0.506으로 떨어져 운영 지표에서도 손해다.
  결정 구조는 이 경계의 원인이 아니다.

  함께 잰 오라클 상한이 캐스케이드의 실패를 설명한다. 경계 두 라벨만 남기고 학습하면
  정확도가 word+char 0.712 / char 0.720으로 다수 클래스 기준선 0.527을 크게 넘는다.
  경계에 신호가 없는 것은 아니다. 그러나 이 값은 **진짜 경계 건만 넣어준 오라클 라우팅**
  이고, 캐스케이드는 1단계가 잘못 보낸 건을 받고 놓친 건은 받지도 못해 오차가 곱해진다.
  그래서 네 구성 중 캐스케이드가 가장 낮다.

- 관측 — **앙상블 멤버 태그가 실행을 조용히 버리고 있었다.** `load_members`가 크기와
  seed를 뭉뚱그려 `ftL`·`ft42`·`ftM`으로만 구분해, 같은 태그에 여러 실행이 들어오면
  마지막 하나만 남았다. roberta-large seed 7·13, 마스킹 seed 3개, KoELECTRA, KoBigBird가
  그렇게 사라져 멤버가 16개 중 8개만 보였다. 더 심각한 것은 **2분류 실행이 3분류 투표에
  섞여 들어간 것**이다 — 예측이 `검토필요`라 어느 3분류 라벨과도 맞지 않아 단독 통합 OOF가
  0.270으로 찍힌다. 태그를 `모델 계열+크기+seed+마스킹`으로 유일하게 만들고, 2분류 실행은
  건너뛰며, 태그가 겹치면 덮어쓰지 않고 멈추도록 고쳤다. 단일 모델 표도 손으로 적은 목록
  대신 실제 멤버 전부를 싣는다. 기록된 최고 조합은 그대로 재현된다
  (`wc+ftB7+ftL42` 통합 OOF 0.6773 / fold 평균 0.6428 / 오답 255 / 경계 97).

- 관측 — **멤버를 다 꺼내도 최고 점수는 오르지 않고, 정직한 추정치는 내려간다.** 멤버
  8개에서 16개로, 후보 조합 16개에서 680개(크기 3 전수)로 넓혀 다시 골랐다. 최고는
  여전히 `wc+ftB7+ftL42` 0.6773이다. 그런데 같은 중첩 선택을 후보 680개로 돌리면
  통합 OOF가 0.677에서 **0.650**으로 내려간다(오답 276, 경계 110). 조합을 더 열심히
  찾을수록 선택 낙관이 커진다는 뜻이며, 가이드 02 §1이 경고한 선택 누수가 후보 수에
  따라 실제로 커지는 것을 보여준다. **조합 탐색은 점수를 짜낼 방향이 아니다.**

- 논문 메모(보관용):
  - 경계가 움직이지 않는 이유를 이제 세 층에서 확인했다 — 모델 계열·용량·입력(9/2),
    결정 구조(오늘), 라벨 재현성(9/3 경계 100건). 세 방향 모두에서 같은 자리에 멈춘다.
  - 오라클 0.712와 실제 캐스케이드 0.596의 간격은 **라우팅 오차가 곱해지는 비용**이다.
    계층 구조를 제안할 때 상한만 보고 이득을 계산하면 안 된다는 사례로 쓸 수 있다.
  - 3분류를 유지하는 한 현실적인 천장은 통합 OOF 0.65~0.68이다. 20가지 변형(가이드 02의
    16가지 + 오늘 4가지)에서 채택한 것이 없다.

- 산출물: `scripts/evaluation/decision_structure.py`, `tests/test_decision_structure.py`,
  `notebooks/18_decision_structure.ipynb`, `scripts/evaluation/finetune_ensemble.py`의
  `member_tag`·2분류 제외·태그 충돌 검사, `tests/test_finetune_ensemble.py` 추가분,
  `reports/current/v4/finetune_results.{md,json}` 재생성분(멤버 8 → 16).

## 2026-09-06 21:30

- 결정: **데이터 확충을 다음 방향으로 삼되 문서 3~5개에서 한 번 끊는다.** 학습 문서 수에
  대한 학습곡선을 재보니 아직 평평하지 않다. 목표를 미리 10개·20개로 정하지 않고, 3~5개를
  더 모아 곡선을 다시 적합한 뒤 계속할지 판단한다. 아울러 저장소를 정리했다.

- 이유: 20가지 모델 변형(가이드 02의 16가지 + 2026-09-06 19:24의 4가지)에서 채택한 것이
  하나도 없다. 남은 방향이 데이터인지 확인하기 위해, 학습 문서를 2~8개로 줄여가며 같은
  평가 문서에서 macro F1을 쟀다(문서 조합을 무작위로 여러 번 뽑아 평균, 총 260회 평가,
  `word+char + balanced Logistic`).

  | 학습 문서 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
  |---|---|---|---|---|---|---|---|
  | macro F1 | 0.4672 | 0.5088 | 0.5423 | 0.5551 | 0.5959 | 0.5937 | 0.6144 |

  8개 지점이 기록된 기준선 0.6144와 정확히 일치해 계산이 맞는 것을 확인했다. 가중 선형
  기울기는 문서 1개당 **+0.0236 ± 0.0056**으로 0에서 확실히 떨어져 있다. 가중 로그 적합은
  `macroF1 = 0.1051·ln(학습문서수) + 0.3934`이고 가중 R² 0.974, 잔차가 ±0.014 안에 든다.

  로그 적합으로 목표별 필요 문서 수를 내면 0.65에 총 13개(+3), 0.68에 17개(+7),
  0.70에 20개(+10), 0.75에 32개(+22)다. **로그 곡선이라 위로 갈수록 급격히 비싸진다.**
  95% 구간은 목표가 높을수록 넓어지므로(0.75는 +8~+76) 높은 목표의 예측은 쓰지 않는다.

  비용 구조가 예상과 달랐다. 문서 1개(중앙값 95건) 3회 반복 라벨링은 Claude Sonnet 5
  배치로 **$1.72**, 5개를 더해도 $9다. 병목은 API 비용이 아니라 **적합한 RFP를 찾는 일과
  표 추출**이다. 모델을 딥시크·업스테이지로 바꾸는 선택이 실제로는 $9 대 $1의 문제이므로,
  004(실행 경로 교란)를 새로 만드는 대가가 절감액보다 크다. 모델은 바꾸지 않는다.

- 수집 조건(현재 10문서의 프로필에서 도출): (1) **AI·데이터 플랫폼 구축 사업**일 것 —
  도메인은 국방·금융·의료로 이미 넓지만 사업 성격은 10개가 전부 같다. (2) 요구사항이
  **번호 붙은 표**로 정리돼 있을 것 — 현재 추출기가 마크다운 표를 파싱한다
  (`labeled_id` 938건 / `header_id` 86건). (3) **50건 이상** — 현재 최소가 49건(korail)이고
  그보다 작으면 fold 하나가 무의미해진다. (4) 라벨 분포가 극단적이지 않을 것 — 통상수용
  비율이 이미 28~79%로 넓다(003).

  이 곡선은 새 문서가 기존 10개와 **같은 종류**라는 가정 위에 있다. 성격이 다른 문서를
  넣으면 곡선대로 오르지 않고 fold 편차만 커질 수 있다.

- 정리 — **`reports/` 루트가 통째로 죽은 사본이었다.** 버전별 레이아웃
  (`reports/current/{version}/`)이 생기기 전 산출물이 8.7MB 남아 있었고, 현재 코드 중
  루트에 쓰는 것은 하나도 없었다(루트 파일 최종 갱신 08-27, v4 대응본 08-28~09-01).
  8개를 지우고 `experiment_environment.json`만 v4로 옮겼다. 이때 `explanation_viewer.py`의
  기본 출력이 그 루트 경로였음이 드러나 다른 보고서 스크립트와 같은 관례로 맞췄다
  (`reports/current/{version}/explanation_viewer.html`). README의 링크도 함께 고쳤다.
  4MB짜리 생성 HTML은 gitignore하고 재생성 명령을 README에 적었다 — 원본
  `model_explanations.json`은 계속 커밋한다. 참조가 하나도 없는 일회성 분석 스크립트
  5개를 지웠다(`analyze_chunk1`·`analyze_consistency`·`compare_100_pool`·`compare_pools`·
  `inspect_anchor_chunk1`). `reports/` 38MB → 29MB, 추적 파일 275 → 261개.

- 정리 — **실험 추적 프레임워크를 도입하지 않는다.** `mlflow`가 requirements에 있었으나
  코드에서 한 줄도 쓰지 않았고 가이드 04는 "필요 없다"고 적어두었다. 제거했다. MLflow는
  실행마다 고유 id를 주므로 19:24 항목의 태그 충돌을 막아줬겠지만, 결과를 gitignore된
  `mlruns/`로 빼낸다. **이 저장소의 규율은 결정 기록이 커밋된 보고서를 인용하는 것**이라
  실험 기록이 버전 관리 밖으로 나가면 그 구조가 깨진다. `finetune_runs.jsonl`은 이미
  append-only에 config가 동봉된 최소 트래커이고 빠진 것은 유일 키 하나뿐이었다.
  `scripts/modeling/run_id.py`를 만들어 **기록할 때** 이름을 박도록 했다
  (`roberta-large.s42.len512.v4`, 마스킹·2분류는 접미사). 읽는 쪽에서 설정을 보고
  되만들면 규칙이 어긋날 때 조용히 겹치므로, 쓰는 시점으로 옮긴 것이 요점이다.

- 남은 판단: `data/processed/`가 gitignore라 **입력 데이터
  `requirements_v0.2.0.jsonl`이 버전 관리에 없다.** 반면 `data/labels/`는 추적된다.
  결정 기록이 입력 파일의 sha256을 인용하는데 그 파일이 저장소에 없는 상태다.
  추적하면 git이 +5MB 늘어난다. 이번에는 손대지 않았다.

- 산출물: `scripts/modeling/run_id.py`, `scripts/evaluation/explanation_viewer.py`의
  버전 폴더 기본값, `.gitignore`·`README.md`·`requirements.txt` 갱신.

## 2026-09-07 03:04

- 결정: 새 RFP 3건을 넣어 `requirements_v0.4.0`(1,445건 · 13문서)과
  `label_dataset_v5`(같은 1,445건, 프롬프트 v5 · `anchor_pool_v2` · 층화 인출)를
  만들고 **동결한다.** `label_dataset_v4`는 그대로 둔다. 이어서 같은 요구사항 전수를
  프롬프트 v6c·zero-shot으로 한 번 더 라벨링해 `label_dataset_v6` 후보를 만든다.
  **Upstage Solar는 채택하지 않는다.**

- 이유 — **데이터 확충이 20가지 모델 변형 중 처음으로 점수를 올렸다.**
  `word+char + balanced Logistic` 기준 fold 평균 macro F1이 **0.6144 → 0.6395**
  (+0.025), 검토 recall이 **0.5679 → 0.6051**(+0.037)이다. 09-06 21:30의 학습곡선이
  로그 적합으로 예측한 값은 0.65였다. 외삽이 조금 낙관적이었으나 방향과 크기가 맞았고,
  이제 13문서 지점이 **측정값**이 되어 다음 확충 판단의 근거가 튼튼해졌다.

  | 신규 문서 | 기관 | 건수 | 원본 |
  |---|---|---:|---|
  | `supreme_court_sentencing` | 대법원 | 155 | `.hwp` |
  | `kdhc_plant_integrated_mgmt` | 한국지역난방공사 | 142 | `.xlsx` |
  | `knoc_genai_platform` | 한국석유공사 | 124 | `.hwp` |

  라벨 조건이 동결본과 같아서 한 데이터셋으로 합칠 수 있었다 — 프롬프트 v5, 앵커 풀
  `anchor_pool_v2`(sha256 일치), 층화 인출. 앵커가 실제로 1:1:1로 들어갔는지
  `anchors_used`로 실측 확인했다(920/920/920, 421/421/421). 조건이 하나라도 달랐다면
  문서와 프롬프트가 얽혀 004를 새로 만들었을 것이다. manifest에
  `labeling_conditions`를 새로 넣어 이 조건을 파일에 박았다 — **데이터셋 번호와
  프롬프트 번호는 다른 축이다**(`label_dataset_v4`도 프롬프트 v5로 만들어졌다).

  신규 421건의 라벨 분포는 기존과 같은 종류다. 문서별 통상수용 비율이 45.2~69.0%로
  기존 범위(28.4~79.2%) 안에 들어오고, 결정 21 고정 규칙 불일치가 0.7%(3건)로 낮다.
  `domain_dependency` 높음만 8.8% → 15.4%로 늘었는데 사법·플랜트·에너지라는 성격상
  자연스럽다.

- 관측 — **원본에서 직접 표를 읽는 편이 Markdown 변환본보다 안전하다.** PIPELINE.md는
  변환을 "자동화돼 있지 않다"고 적었고 decisions-01 §11.2는 그 단계에서 중첩 표의 셀
  경계가 붙는 위험을 경고했다. HWP 5.0은 OLE2 복합 문서이고 셀마다 행·열 좌표가 레코드에
  있어 표 구조를 손실 없이 복원할 수 있다. `scripts/data/hwp_tables.py`·`xlsx_tables.py`가
  `표 → 행 → 셀`을 내놓고 `build_dataset`의 뒷단을 그대로 쓴다.

  작업 중 **조용히 건수를 깎던 결함 셋**을 잡았다.
  (1) HWP 확장 제어문자는 8워드를 차지하는데 건너뛰지 않으면 컨트롤 id의 ASCII
  바이트쌍이 한자로 읽혀 `汤捯QMR-013` 같은 값이 본문에 섞인다. 처리 전후로 한 문서의
  요구사항 인식이 **37건에서 119건**으로 달라졌다.
  (2) `find_requirement_id`가 `요구사항고유번호`만 알았다. 대법원은 `요구사항 번호`로
  적어 문서 전체를 놓칠 뻔했다. 판정이 두 함수에 흩어져 있어 `is_id_label()`로 모았다.
  (3) XLSX 정의서의 곁다리 열(`응락수준`)과 미러 열이 값에 섞여 유형이
  `'기능 요구사항\n응락수준\n필수\n기능 요구사항'`으로 나왔다. 라벨·값 두 칸으로 줄여
  정규화가 정상 동작한다.

  복합 ID(`SFR-SIM-012`)를 단순 정규식으로 세면 대법원이 155건에서 105건으로 줄어든다.
  저장소의 `ID_RE`가 이를 올바로 처리하고 `prefix_of()`가 첫 토큰만 떼므로 유형 정규화도
  정확하다. `DPR`(데이터 플랫폼 요구사항) 매핑을 추가했고, `COM` 2건은 원본이 성격이
  다른 둘을 `공통` 한 칸에 넣어 가를 수 없으므로 `기타`(`source="none"`)로 둔다.
  taxonomy가 "조용히 최빈값으로 채우지 않는다"고 정한 자리이며 `기타`의 첫 사용례다.

- 관측 — **Upstage Solar Pro 4는 이 작업에 맞지 않는다.** 전수 1,445건에 v6c 프롬프트로
  돌려 454건을 얻었다(나머지는 rate limit). 표본은 문서별 성공률 23~37%로 고르게 퍼져
  편향되지 않았다. 기각 근거는 일치율이 아니라 **지시 이행**이다.

  | | 동결 v5 | 신규 v5 | Claude v6c | **Solar v6c** |
  |---|---:|---:|---:|---:|
  | 결정 21 규칙 불일치 | 0.0% | 0.7% | 2.8~3.1% | **10.4%** |
  | 「」 인용률 | – | – | 81% | **29%** |
  | 계약·질의검토 비중 | 26.4% | – | 16~17% | **9.0%** |

  규칙 불일치와 인용은 옳고 그름이 있는 항목이다. `[판단 순서]`가 명시돼 있는데 `cost_basis`를
  채우고 `통상수용`이라 답한 건이 18건, `blockers`가 있는데 `견적반영`이라 답한 건이 18건이다.
  동결 v5와의 일치율은 61.0%(Wilson 55.7~66.1)로 기준선(동일조건 반복 92.5%)과 구간이
  전혀 겹치지 않는다. 명목 단가가 싸도 라벨의 신뢰도를 살 수는 없다.

- 관측 — **Upstage 캐시는 프리픽스가 아니라 전체 프롬프트 일치 방식이다.** `prompt_cache_key`가
  "명시적 키"라는 것은 문서에 있지만, 그것이 프리픽스가 아니라는 사실은 실측으로만 드러났다.
  같은 입력을 두 번 보내면 1,767토큰 중 1,760(99.6%)이 캐시에서 읽히지만, 본문이 건마다
  다른 실제 워크로드에서는 **적중 0%**다. Anthropic은 같은 조건에서 시스템 프롬프트가
  캐시된다(`batch_chunk2` 실측 249,561토큰). **모델 비교에서 명목 단가만 보면 안 된다.**

- 반성 — 두 가지를 잘못했다. 기록으로 남긴다.
  (1) **확인 없이 유료 배치를 제출해 $3.83을 썼다.** 그 시점에 프롬프트 v5/v6c 우열이
  미결이었고 사용자가 Upstage를 시험해보자고 했으며 요청은 "리라벨링"이었는데 실행한 것은
  "신규 문서 최초 라벨링"이었다. `run_claude_batch.py`에는 `--execute`가 없으면 dry-run만
  하는 안전장치가 있었는데 그 플래그를 직접 붙여 통과시켰다. 결과적으로 그 라벨이
  `label_dataset_v5`가 됐지만, 결과가 좋았다는 것이 절차를 정당화하지 않는다.
  (2) **Solar 러너의 재시도에 백오프를 넣지 않아** 429가 972건 났다. 429는 감속 신호인데
  즉시 재시도해 악화시켰다. 동시성 8 → 2, 재시도 3회 즉시 → 6회 지수 백오프(`retry-after`
  우선, 지터 포함)로 고쳤다.

- 조치 — **배치 기록이 실행 조건을 남기게 했다.** `batch_info.json`에 `prompt_version`,
  `prompt_sha256`, `model`을 넣었다. 이것이 없어서 08-19 실행의 조건을 git 이력으로
  되짚어야 했고(09-06 18:08), 그 과정에서 "프롬프트가 같다"를 기록이 아니라 추론으로
  주장하게 됐다. 아울러 main의 러너에 zero-shot(`--retrieval none`)과 프롬프트 파일
  지정(`--prompt`)을 옮겨 v6 계열 실험을 aux-heads 없이 돌릴 수 있게 했다.

- 진행 중: `msgbatch_01NFvmTgJE4WnzWqaA81U8z6` — 1,445건, 프롬프트 v6c
  (sha256 `77d8f8de…`), zero-shot, 힌트 없음, 1회. 예상 $4.40. 받으면
  `label_dataset_v6` 후보가 되고, v5와의 대조로 프롬프트 효과를 잰다. **힌트는 289건분만
  있어 전수에 못 썼으므로, Claude v6c 289건과 비교할 때는 모델이 아니라 힌트 유무가
  다르다는 점을 함께 읽어야 한다.**

- 논문 메모(보관용):
  - 데이터 확충은 모델 변형 20가지가 모두 실패한 뒤 학습곡선이 지목한 유일한 방향이었고,
    실제로 유일하게 점수를 올렸다. 확충의 근거를 곡선으로 제시하고 13문서 지점에서
    예측(0.65)과 실측(0.6395)을 나란히 보고한다.
  - 저가 모델 교체 실험의 기각 근거로 **지시 이행률**(규칙 불일치·인용률)을 쓴다.
    일치율만 보면 "판단이 다르다"로 읽히지만, 규칙과 인용은 옳고 그름이 있다.
  - 추출 파이프라인에서 잡은 세 결함은 모두 건수를 조용히 깎았다. 원본 대조 없이
    추출기만 믿으면 알 수 없다는 decisions-01 §3 자동 검수의 실제 사례다.

- 산출물: `scripts/data/hwp_tables.py`, `scripts/data/xlsx_tables.py`,
  `scripts/labeling/run_solar_labeling.py`, `tests/test_hwp_tables.py`,
  `tests/test_xlsx_tables.py`, `data/processed/requirements_v0.4.0.{jsonl,csv}`,
  `data/labels/label_dataset_v5.{jsonl,_manifest.json}`(sha256 `5ca9e2ea…`),
  `reports/current/solar_runs/full_v0.4.0_v6c/`(454건),
  `reports/current/claude_batches/{new_docs_v0.4.0,full_v0.4.0_v6c}/`.
