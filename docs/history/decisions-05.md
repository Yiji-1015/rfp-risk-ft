# 연구 결정 기록 05

## 2026-08-25 15:08

- 결정: 남동발전 요구사항 101건의 빈 `requirement_type`을 원문 PDF의 11개 구역 제목과
  요구사항 ID 접두어의 대응으로 채우고, `requirements_v0.3.0` 및
  `label_dataset_v3`으로 새 버전을 만든다. 이전 동결본은 수정하지 않는다.
- 이유: 상세표 자체에는 유형 열이 없지만 원문은 각 표 묶음 바로 앞에서 유형을 명시한다.
  r2 PDF와 저장소 PDF의 요구사항 구간을 대조했고, 101건의 접두어 분포가 각 구역과
  충돌 없이 일치했다. v2 대비 변경 필드는 101건의 `requirement_type`과
  `requirement_type_source`뿐이며 라벨·본문은 동일하다.
- 논문 메모: 결측을 접두어만으로 추정한 것이 아니라 문서 구역 제목에서 복원했다.
  따라서 유형 근거는 `prefix` 101건에서 `text` 101건으로 바뀌며 전체 1,024건이
  `text`가 된다. 모델 입력은 요구사항 본문뿐이므로 기존 성능 결과는 변하지 않는다.

## 2026-08-25 15:37

- 결정: 유형 단독 모델은 만들지 않고, 정규화한 요구사항 유형 12종을 one-hot으로 만들어
  문자 TF-IDF 및 단어+문자 TF-IDF에 각각 결합했다. 원본 유형 표기 60종은 문서별 표현을
  직접 드러내므로 특징으로 쓰지 않았다.
- 이유: 사용 목적은 유형만으로 라벨을 맞히는 것이 아니라 본문 모델에 유형 맥락을 더했을
  때 일반화가 좋아지는지 확인하는 것이다. 같은 8/1/1 LODO와 balanced Logistic을 유지해
  유형 추가만 통제 비교했다.
- 논문 메모: 문자+유형은 macro F1 0.549, 정확도 0.616, 계약 recall 0.445로 문자 단독
  대비 -0.051(-0.159~+0.011), 우세 2/10이었다. 단어+문자+유형은 macro F1 0.558,
  정확도 0.623, 계약 recall 0.490으로 단어+문자 대비 -0.046(-0.180~+0.045), 우세
  1/10이었다. 유형은 문서별 라벨 분포와 얽혀 처음 보는 문서에서 불안정한 힌트가 된다.
  분석 메타데이터로는 유지하되 현재 모델 입력에서는 제외한다.

## 2026-08-25 16:38

- 결정: 유형 one-hot 블록 가중치 후보 `0 / 0.05 / 0.1 / 0.25 / 0.5 / 1.0`을 각 fold의
  학습 8문서로 학습하고 검증 1문서의 macro F1으로 선택했다. 동률이면 작은 가중치를
  택했으며 평가 문서는 선택에 사용하지 않았다.
- 이유: 고정 가중치 1에서 유형 결정 점수의 RMS가 본문보다 평균 1.327배 커, 불안정한
  유형 신호가 본문을 덮는다는 진단을 직접 검증하기 위해서다.
- 논문 메모: 문자+유형은 0.549에서 0.589로 회복했지만 문자 단독 0.601보다 -0.011
  (-0.159~+0.025), 우세 4/10이었다. 선택 가중치는 0/0.05/0.1/0.25/1.0이
  3/3/2/1/1개 fold였다. 단어+문자+유형은 0.558에서 0.584로 회복했지만 기준선
  0.603보다 -0.019(-0.159~+0.013), 우세 2/10이었다. 작은 가중치로 과대 영향은
  완화되지만, 검증 문서 하나에서 추정한 유형 효과가 다음 문서로 일반화되지 않는다.
  유형 특징은 채택하지 않는다.

## 2026-08-25 16:56

- 결정: 문자 TF-IDF 기준선에 `log(1+글자 수)`, 숫자 유무와 `log(1+숫자 개수)`, 그리고
  두 묶음의 결합을 각각 추가했다. 줄 시작의 `1)`, `2.` 같은 목록 번호는 숫자 집계에서
  제외했다. 목록 개수·단위·기준 표현은 문서 서식이나 표현 중복 위험 때문에 넣지 않았다.
- 이유: 본문 TF-IDF가 직접 표현하기 어려운 요구사항 크기와 정량 조건 밀도가 새 문서에도
  일반화되는지 한 번에 하나씩 통제 비교하기 위해서다. 수치 열의 표준화는 학습 fold에서만 했다.
- 논문 메모: 글자 수는 macro F1 0.571(기준선 대비 -0.030, 우세 2/10), 숫자 정보는
  0.587(-0.013, -0.111~+0.038, 우세 6/10), 전체 결합은 0.575(-0.026, 우세 3/10)였다.
  숫자 정보는 여러 문서에서 소폭 이득이 있지만 일부 문서의 큰 하락을 상쇄하지 못한다.
  현재 데이터에서는 세 구조·수치 feature를 채택하지 않고 문자 TF-IDF 0.601을 유지한다.

## 2026-08-25 17:22

- 결정: 본문 문자 TF-IDF와 본문에서 계산한 글자 수·숫자 정보만 유지한 채 Elastic-net
  Logistic, SVD100+Logistic 대조군, SVD100+XGBoost를 고정 초기 설정으로 비교했다.
  XGBoost에는 학습 fold의 클래스 분포로 계산한 balanced sample weight만 사용했다.
- 이유: 선형 모델에서 악화된 구조·수치 정보가 희소 선택이나 비선형 조건부 결합에서는
  유효한지 확인하되, SVD와 분류기 효과를 분리하기 위해 SVD+Logistic을 함께 뒀다.
- 논문 메모: Elastic-net은 macro F1 0.470(문자 기준선 대비 -0.131,
  -0.260~-0.049, 우세 0/10), SVD+Logistic은 0.532(-0.069, -0.197~+0.045,
  우세 1/10), SVD+XGBoost는 0.563(-0.038, -0.213~+0.079, 우세 3/10)이었다.
  XGBoost가 SVD Logistic보다 평균 0.031 높아 비선형 결합의 회복은 관찰됐지만, SVD로
  희소 문자 단서를 압축한 손실을 상쇄하지 못했다. 하이퍼파라미터 선택과는 별도 결과다.

## 2026-08-25 17:37

- 결정: 본문 문자 TF-IDF와 동결 E5를 결합하고, E5 블록 가중치 후보
  `0 / 0.05 / 0.1 / 0.25 / 0.5 / 1.0`을 fold별 검증 1문서의 macro F1으로 골랐다.
  같은 절차에 본문 유래 숫자 유무·개수를 추가한 모델도 분리 비교했다.
- 이유: 문자 TF-IDF의 정확한 표면 표현과 E5의 의미 표현이 상보적인지 확인하고, 숫자
  정보의 효과는 결합 모델과 분리해 측정하기 위해서다. 평가 문서는 선택에 쓰지 않았다.
- 논문 메모: TF-IDF+E5는 macro F1 0.611, 정확도 0.673, 계약 recall 0.508이었다.
  문자 단독 대비 +0.010(-0.029~+0.045), 우세 5/10이므로 숫자상 최고지만 안정적인
  개선으로 확정하지 않는다. 선택 가중치는 0/0.5/1.0이 각각 3/3/4개 fold였다.
  숫자 추가 모델은 0.592(문자 단독 대비 -0.009, -0.102~+0.047, 우세 6/10)로,
  숫자 없는 결합보다 평균 0.019 낮았다. 숫자 feature는 제외하고 TF-IDF+E5를 유망
  후보로 유지한다.

## 2026-08-25 17:56

- 결정: 숫자 집계에서 줄 시작의 `1 |` 같은 표 행 번호도 제외하도록 보정하고, 숫자
  feature가 포함된 실험 6종을 모두 재실행했다. 본문·분할·나머지 설정은 유지했다.
- 이유: 16개 요구사항에서 표 행 번호가 정량 조건으로 잘못 집계되어 문서 서식이 숫자
  feature에 섞였다. 앞선 결정 기록은 보존하고 이 보정 결과를 현재 수치로 사용한다.
- 논문 메모: 숫자 단독은 macro F1 0.591(-0.009, -0.098~+0.047, 우세 6/10),
  글자 수+숫자는 0.572(-0.029, -0.157~+0.031, 우세 3/10), Elastic-net은
  0.471(-0.129, -0.260~-0.071, 우세 0/10), SVD+Logistic은
  0.528(-0.072, -0.245~+0.020, 우세 1/10), SVD+XGBoost는
  0.564(-0.037, -0.213~+0.122, 우세 3/10)이었다. TF-IDF+E5+숫자는
  0.594(-0.007, -0.102~+0.066, 우세 7/10)로 숫자 없는 결합 0.611보다 0.017 낮았다.
  결론은 동일하다. 숫자 feature는 채택하지 않고 TF-IDF+E5를 유망 후보로 유지한다.

## 2026-08-26 00:39

- 결정: 앵커 풀과 평가 설계의 관계에서 13:59 수정으로 지워지지 않는 두 관측을
  `docs/issues/009`, `docs/issues/010`으로 분리해 기록한다. 재라벨링도 앵커 풀 수정도
  하지 않는다. 009는 수용(측정 불가), 010은 미해결(사람확정 2건 원문 대조)이다.

- 이유 1 — **교차 fold 경로는 남는다.** 13:59 결정으로 동결 앵커 100건이 검증·평가에서
  빠져 LODO 평가 합계가 924건이 됐지만, 앵커는 학습 8문서에 남는다. 결정 10이 차단한
  것은 "타깃 → 자기 문서 앵커"뿐이므로, fold `i`에서 평가 문서 `D`의 앵커는 나머지
  9문서의 라벨을 만드는 프롬프트에 정상적으로 들어갔다. 실행 기록의 `anchors_used`로
  세면 그 비율이 **16.5%(kac) ~ 44.1%(kexim)**다. kexim fold는 학습 데이터의 44%가
  kexim 요구사항과 그 라벨을 예시로 보고 만들어졌다. 빠지는 것은 앵커 행이고,
  남는 것은 그 앵커가 학습 라벨에 남긴 흔적이다.

  008과 겹쳐 읽어야 한다. 인출 유사도 중앙값이 0.090이라 앵커는 유사 사례가 아닌데,
  결정 28은 어떤 앵커가 들어가는지가 판정을 76.9%까지 벌린다고 관측했다. 약한 상관이
  아니라 라벨 값을 실제로 움직이는 경로다. 방향은 특정할 수 없어 측정 불가로 남긴다.

- 이유 2 — **앵커 풀 라벨과 동결 라벨이 16건 다르다.** `anchor_pool_v2`와
  `label_dataset_v3`을 uid로 대조하면 주 라벨이 16/100건 어긋난다(`실버_3of3` 12/80,
  `모델일관_3of3` 2/9, `사람확정` **2/11**). 규칙 보정으로 설명되는 것은
  `defense:ECR-029` 한 건뿐이다. 앵커 100건은 3회 만장일치를 통과한 가장 안정적인
  부분집합인데 16%가 뒤집혔고, 이는 결정 23의 반복 변동 7.5%의 약 2배다.
  이 16건은 13:59 결정에 의해 평가에서 제외되고 학습 8문서에만 남으므로,
  점수를 직접 오염시키지 않고 **학습 라벨의 품질 문제**로 작용한다(004와 같은 급).

- 이유 3 — **골드 11건을 우열 판정에 쓰지 않는다.** 결정 21의 사람확정 11건과 현재
  라벨의 일치율은 9/11(81.8%)로 결정 21 시점과 같지만 **맞은 항목이 다르다.**
  blocker 5범주가 `kac:AIP-001`·`koen:CON-003`을 고쳤고 `defense:QUR-001`·
  `mfds:SFR-001`이 새로 틀렸다. 11건 중 4건이 흔들렸으므로, 결정 22·23·28에서
  zero-shot 10/11 대 few-shot 9/11로 적었던 차이는 신호가 아니다. Wilson 95% CI가
  (0.523–0.949)와 (0.623–0.984)로 거의 완전히 겹친다. 반복 일관성 쪽도 37/40 대
  36/40으로 1건 차이이며 CI가 (0.801–0.974)와 (0.769–0.960)로 겹친다.
  **전수 데이터셋을 층화 few-shot으로 만든 선택이 40건 위의 1건 차이에 근거했다는
  사실을 논문에 명시한다.** 데이터셋은 바꾸지 않는다.

- 부수 관측 — **앵커 제외가 fold 크기 편차를 키웠다.** 앵커는 문서마다 7~13건으로
  거의 균일한데 문서별 요구사항 수는 49~192건이라, 제외 타격이 작은 문서에 집중된다.
  fold 크기 범위가 49~192(3.9배)에서 **37~181(4.9배)**로 넓어졌다(korail -24.5%,
  kangwon -18.0%, mfds -5.7%). 003이 지적한 편차가 커졌으므로 결정 33이 정한
  "fold 단순 평균과 건수 가중을 둘 다 보고한다"의 근거가 더 강해진다.

- 논문 메모:
  - 008·009·010은 독립 결함이 아니라 하나의 한계다 — **앵커 기반 LLM 라벨링은
    라벨셋과 평가 설계를 분리해 두지 못한다.** 개별 항목이 아니라 이 문장으로 보고한다.
    2026-08-23 08:35에서 003과 006이 독립이 아님을 관측한 것과 같은 종류의 정리다.
  - 열려 있는 유일한 작업은 사람확정 2건(`defense:QUR-001`, `mfds:SFR-001`)의
    원문 대조다. 실무자 판단이 여전히 옳으면 §10.3 감사 표본에 오라벨로 기록하고,
    규칙이 바뀌어 뒤집힌 것이면 결정 21의 규칙을 갱신한다.
  - 확장 감사를 한다면 무작위 표집이 아니라 **불일치 16건 + 사람확정 11건**을 우선
    표본으로 둔다. 무작위보다 정보량이 높다.

## 2026-08-26 08:10

- 정정: 00:39에 `docs/issues/009`로 등록한 "동결 앵커가 학습 문서의 라벨 생성에
  관여한다"를 **철회한다.** 이슈 목록에서 빼고 여기에 측정 기록으로만 남긴다.
  010(앵커 라벨 ≠ 동결 라벨)은 유지한다.

- 철회 이유 1 — **의도된 설계를 결함으로 적었다.** 앵커가 라벨 생성에 관여하는 것은
  결정 9·13·14·18이 정한 방법 그 자체다. 결정 10이 차단하기로 한 것은 "타깃과 같은
  문서의 앵커" 하나이며 그것은 위반 0건으로 지켜졌다. 평가 문서의 앵커가 다른 문서의
  프롬프트에 들어가는 것은 막기로 한 적이 없다. §8.4의 fold별 풀 구성은 앞으로의
  개선 항목이지 지금 데이터의 결함이 아니다.

- 철회 이유 2 — **fold별 편차 16.5~44.1%가 교란의 증거가 아니다.** 층화 인출이
  기계적으로 만들어내는 값과 대조하지 않고 관측값만 적었다. 풀이 라벨별 44/25/31건이고
  문서당 7~13건이므로, 라벨마다 1건씩 뽑을 때 D의 앵커가 한 번이라도 들어갈 확률을
  타깃 문서별로 계산해 행 수로 가중하면 다음과 같다.

  | 평가 문서 D | 관측 | 귀무기대 | 비 |
  |---|---:|---:|---:|
  | koen_ai_infrastructure | 42.1% | 27.6% | **1.53x** |
  | kexim_ai_platform | 44.1% | 37.4% | 1.18x |
  | incheon_airport_digital_work | 29.5% | 25.5% | 1.16x |
  | korail_genai_isp_ismp | 33.2% | 31.7% | 1.05x |
  | defense_intelligent_platform | 43.0% | 43.0% | 1.00x |
  | kangwon_land_genai | 28.0% | 30.3% | 0.92x |
  | genai_incident_response | 20.5% | 25.2% | 0.81x |
  | mfds_drug_ai_review | 23.8% | 30.7% | 0.78x |
  | ccrs_ai_platform | 19.2% | 25.0% | 0.77x |
  | kac_ai_work_platform | 16.5% | 29.1% | 0.57x |

  **기대값 자체가 25.0~43.0%로 벌어져 있다.** 관측 편차의 대부분은 층화 인출과
  문서별 앵커 수(7~13건)의 구조에서 나온다. 비로 보면 0.57~1.53x이고 중앙이 1 근처다.
  003·006처럼 "fold마다 다른 교란"으로 병기할 근거가 되지 못한다.

  koen만 1.53x로 떠 있다. 인프라 증설 사업이라 표준 문구가 많고 통상수용이 많은
  문서인데(003), 풀의 `통상수용` 44건에서 koen 몫이 실제 인출에서 더 자주 걸린 것으로
  보인다. 단일 관측이므로 원인으로 단정하지 않고 기록만 한다.

- 남는 사실 하나 — **앵커 제외가 fold 크기 편차를 키웠다.** 이것은 00:39 항목에서
  유효한 부분이다. 앵커는 문서당 7~13건으로 거의 균일한데 문서별 요구사항 수는
  49~192건이라 작은 문서가 더 깎인다. fold 크기 범위가 49~192(3.9배)에서
  **37~181(4.9배)**로 넓어졌다(korail -24.5%, kangwon -18.0%, mfds -5.7%).
  결정 33이 정한 "fold 단순 평균과 건수 가중을 둘 다 보고한다"의 근거가 더 강해진다.

- 논문 메모:
  - 00:39 항목은 지우지 않는다. **관측은 옳았고 해석이 틀렸다.** 숫자를 먼저 보고
    귀무기대와 대조하지 않은 채 "교란"이라는 이름을 붙였다. 2026-08-20 결정 34에서
    문서 간 문구 반복을 "누수"라고 불렀다가 철회한 것과 같은 실수이며, 같은 방식으로
    남긴다.
  - 교훈은 **fold별 편차를 보고할 때는 그 편차가 설계에서 구조적으로 나오는 폭을
    먼저 계산한다**는 것이다. 003의 라벨 분포 편차와 006의 반복 노출률에도 같은
    질문을 적용할 수 있다.

## 2026-08-26 08:18

- 결정: `docs/issues/010`의 상태를 미해결에서 **수용**으로 내리고, 사람확정 2건의
  원문 대조도 하지 않는다. 이 이슈에 열려 있는 작업은 없다. 함께 `AGENTS.md`에
  "Before reporting a problem" 절과 git 상태 확인 규칙을 추가한다.

- 010을 내린 이유: 불일치 2건(`defense:QUR-001`, `mfds:SFR-001`)은 둘 다 동결 앵커라
  2026-08-25 13:59 결정에 의해 평가에서 제외되고 학습 8문서에만 남는다. 924건 중 0.2%다.
  요구사항 데이터·앵커 풀·라벨 데이터셋이 모두 동결이므로 대조의 결과가 데이터도 코드도
  논문의 주장도 바꾸지 않는다. 골드 11건 벤치마크는 4/11이 흔들려 우열 판정에 못 쓴다고
  이미 정리했으므로 그 11건을 더 정확하게 만들 실익도 없다.

- 다만 `reasoning`은 읽었고 한 가지를 확인했다 — **남은 두 불일치는 원문 이해 실패가
  아니라 결정 21이 정하지 않은 규칙 경계에서 발생했다.**
  `QUR-001`은 blocker 범주 2("합격 기준 부재")가 합격 기준 미기재만으로 성립하는지
  무제한 재작업 조항이 명시돼야 하는지를 정하지 않은 지점이고, `SFR-001`은 결정 3의
  "고급 기술자·전문 인력 추가 투입" 경계에서 정부 통합인증(Any-ID) 연동이 기본 수행팀
  범위인지를 가르지 않은 지점이다. 규칙을 지금 고치면 이미 생성된 1,024건과 어긋나므로
  고치지 않고, 후속 라벨링을 한다면 이 두 경계를 먼저 정한다.

- `AGENTS.md`를 고친 이유: 최근 세션에서 같은 종류의 혼란이 반복됐다. (a) 파일 수정시각만
  보고 작업이 멈춘 줄 알았는데 작업 트리가 옛 브랜치에 있었고, (b) 오래된 판본의
  결정 파일에 append해 원격 항목을 덮을 뻔했고, (c) 의도된 설계(앵커의 라벨 생성 관여)를
  결함으로 등록했고, (d) fold별 편차를 귀무기대와 대조하지 않고 교란으로 읽었고,
  (e) 결과가 아무것도 바꾸지 않는 항목을 "열려 있는 작업"으로 적었다. 다섯 모두
  **기록을 읽는 방법의 문제**이지 데이터나 코드의 문제가 아니므로, 대응도 개별 문서가
  아니라 진입 지침에 둔다.

- 논문 메모: 한계절에 한 문장 — 남은 사람·모델 불일치는 원문 이해 실패가 아니라
  라벨 규칙이 정하지 않은 경계에서 발생했다. 결정 21이 규칙을 명문화한 뒤에도 회색지대는
  남았고, 사람 검토 11건 규모에서는 그 사실이 드러나지 않았다.
