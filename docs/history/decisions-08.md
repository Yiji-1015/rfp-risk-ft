## 2026-09-07 03:17

- 결정: **`label_dataset_v5`를 주 데이터셋으로 유지하고 v6은 채택하지 않는다.**
  같은 요구사항 1,445건에 프롬프트만 v6c(zero-shot)로 바꿔 전수 라벨링한 결과가
  v5보다 나빴다. v6은 지우지 않고 나란히 둔다 — 프롬프트 효과의 측정 기록이다.

  | 데이터셋 | 프롬프트 | 인출 | macro F1 | 검토 recall |
  |---|---|---|---:|---:|
  | `v4` (1,024건) | v5 | 층화 퓨샷 | 0.6144 | 0.5679 |
  | **`v5` (1,445건)** | **v5** | **층화 퓨샷** | **0.6395** | **0.6051** |
  | `v6` (1,445건) | v6c | zero-shot | 0.5991 | 0.4143 |

- 이유: macro F1이 **-0.040**, 검토 recall이 **-0.191**이다. recall 하락이 특히 크고,
  실무에서는 이쪽이 더 아프다 — 검토가 필요한 건을 놓치는 비율이 그만큼 는다.

  원인은 라벨 분포에 그대로 나온다. `계약·질의검토`가 **25.1% → 12.9%로 반토막**이다.
  v6이 blocker 정의를 "견적으로 덮을 수 없는 손해나 이행 불능"으로 좁힌 결과이며
  의도한 방향이 맞지만, 소수 클래스가 얇아져 macro F1과 recall이 함께 내려갔다.
  **의도한 축소가 점수로는 손해였다.**

  v5와 v6의 주 라벨 일치율은 **1,075/1,445 = 74.4%**(Wilson 95% 72.1~76.6)로 결정 23의
  동일 조건 반복 92.5%(80.1~97.4)와 구간이 전혀 겹치지 않는다. 이동은 한쪽으로 쏠린다 —
  심각도를 내리는 방향이 300건(계약→견적 112, 견적→통상 99, 계약→통상 89),
  올리는 방향이 70건이다.

  **다만 프롬프트와 인출이 함께 바뀌었으므로 둘의 효과는 분리되지 않는다.** 층화 인출은
  라벨당 1건씩 1:1:1을 강제하므로(2026-09-06 19:24 관측 2), 그것이 사라진 것만으로도
  소수 클래스가 줄 수 있다. 이 실험은 "v6c zero-shot 구성 전체"가 v5보다 나빴다는
  것까지만 말한다.

- 이 결과가 **2026-09-06 18:08이 남긴 80건 사람 대조를 대체한다.** 그 항목은 v5와 v6c의
  우열을 가리려면 갈린 80건을 사람이 봐야 한다고 적었다. 전수를 같은 조건으로 돌리니
  점수로 갈렸으므로 그 작업은 필요 없어졌다. 라벨 품질을 사람이 직접 판정하는 일은
  여전히 유효하지만, **프롬프트 선택의 근거로는 더 이상 필요하지 않다.**

- 관측 — **인용 의무와 300자 제한이 충돌한다.** v6c는 근거 원문을 「」로 인용하라고
  요구하는데 `reasoning`은 300자 이내다. 전수 1,445건에서 6건(0.42%)이 스키마 위반으로
  떨어졌고, 289건 실행의 1건(0.35%)과 같은 비율이다. 구조화 출력은 구조를 강제하지만
  **문자열 길이는 강제하지 않는다.**

  6건은 **입력을 그대로 두고 동기로 재시도해 전부 회수**했다(5건은 1회차, 1건은 2회차).
  프롬프트에 "짧게 쓰라"를 덧붙이면 그 6건만 조건이 달라지므로 하지 않았다. 재시도
  비용 $0.16.

- 관측 — **작은 배치의 캐시 수치로 큰 배치를 견적하면 안 된다.** 289건 실행은 건당
  캐시 생성 59토큰 / 읽기 3,335토큰이었는데, 1,445건 실행은 건당 생성 **2,337토큰** /
  읽기 1,057토큰이었다. 캐시를 생성한 건이 69%(991/1,439)다. 배치가 크면 많은 요청이
  캐시가 써지기 전에 동시에 출발해 각자 캐시를 만든다. 캐시 생성은 읽기의 **12.5배**
  단가($1.25/M 대 $0.10/M)라 이것이 비용 초과의 전부였다 — 견적 $4.40, 실제 **$8.38**.

- 상태: 세 데이터셋이 모두 `DATASET_SPECS`에 등록돼 해시 검증을 통과한다.
  `RFP_DATASET_VERSION`으로 바꿔 쓴다. 기본값은 `v4`로 두어 기존 실험 결과가
  그대로 재현되게 한다. v5로 옮길지는 별도 판단이다.

  | key | 파일 | sha256 | 건수 |
  |---|---|---|---:|
  | `v4` | `label_dataset_v4.jsonl` | `f8c1eb25…` | 1,024 |
  | `v5` | `label_dataset_v5.jsonl` | `5ca9e2ea…` | 1,445 |
  | `v6` | `label_dataset_v6.jsonl` | `27f8f2ed…` | 1,445 |

- 논문 메모(보관용):
  - 프롬프트 한 정의를 좁혔더니 의도한 대로 소수 클래스가 줄었고, 그 대가로 macro F1과
    검토 recall이 함께 내려갔다. **라벨 정의 변경은 분포를 통해 점수에 직접 반영된다** —
    프롬프트 설계를 모델 성능과 분리해 논할 수 없다는 사례로 쓴다.
  - 데이터 확충(+0.025)과 프롬프트 변경(-0.040)을 나란히 보고한다. 같은 노력과 비용을
    들여도 방향이 반대일 수 있다.
  - 이 비교는 프롬프트와 인출이 함께 바뀐 것이므로 원인을 하나로 특정하지 않는다.
    분리하려면 v6c를 층화 퓨샷으로 한 번 더 돌려야 하고, 그 값은 아직 없다.

- 산출물: `data/labels/label_dataset_v6.{jsonl,_manifest.json}`,
  `reports/current/claude_batches/full_v0.4.0_v6c/`(1,445건, batch
  `msgbatch_01NFvmTgJE4WnzWqaA81U8z6`), `label_dataset.py`의 `v5`·`v6` 스펙 등록과
  스펙별 건수 검사, `build_label_dataset.py`의 `REPORTS_DIR`·`labeling_conditions`.

## 2026-09-07 03:40

- 결정: `label_dataset_v5`에서 **기준선 자리를 `word+char 3-4gram + balanced Logistic`으로
  유지한다.** TF-IDF 계열 18종, 결정 구조 4종, 2분류, 경계 오라클 상한을 v5로 다시
  돌렸고 순위가 v4와 같았다. 학습곡선도 13문서 지점에서 재적합했다. 파인튜닝·앙상블은
  GPU가 필요해 아직 v4 라벨 기준이며, v5 기준 최고 점수는 gcube 재학습 후에 정해진다.

- 이유 — **데이터가 늘어도 순위가 바뀌지 않았다.** 가이드 02가 "16가지 시도해서 이긴 것이
  없다"고 적은 결론이 1,445건에서도 유지된다.

  | 설정 | v4 (1,024건) | **v5 (1,445건)** | 변화 |
  |---|---:|---:|---:|
  | **word+char + balanced** | 0.614 | **0.640** | +0.026 |
  | char 3-4gram + balanced | 0.601 | 0.634 | +0.033 |
  | Dummy(최빈) | 0.219 | 0.223 | +0.004 |

  `char` 단독이 더 크게 올라 격차가 0.013에서 0.006으로 좁혀졌으나 순위는 그대로이고,
  계약 recall은 0.605 대 0.569로 `word+char`가 확실히 낫다. 유형·길이·숫자·구조 정보를
  더한 변형들은 v4에서와 마찬가지로 기준선을 넘지 못했다(0.591~0.616).

- 관측 — **학습곡선의 외삽이 맞았다.** 09-06 21:30에 학습 문서 2~8개만 보고 만든 로그
  적합이 11문서를 **0.6454**로 예측했고 실측은 **0.6395**였다(오차 +0.006). 이제 13문서
  지점이 측정값이므로 다음 판단은 외삽이 아니다.

  2~11 지점으로 재적합하면 `macroF1 = 0.0896·ln(학습문서수) + 0.4241`(가중 R² 0.969)이고
  기울기가 0.1051에서 **0.0896으로 완만해졌다.** 곡선이 예상대로 눕고 있다.

  | 목표 | 총 보유 문서 | 추가 필요 |
  |---:|---:|---:|
  | 0.65 | 14 | +1 |
  | 0.66 | 16 | +3 |
  | 0.68 | 19 | +6 |
  | 0.70 | 24 | +11 |

  **문서 1개면 0.65, 3개면 0.66이다.** 0.70은 지금의 두 배 가까운 문서가 필요하므로
  현실적인 목표가 아니다. 확충의 수익이 빠르게 줄고 있음을 측정으로 확인했다.

- 관측 — **결정 구조는 v5에서도 움직이지 않는다.** 09-06 19:24이 v4에서 관측한 것이
  그대로 재현됐다.

  | 구성 | macro F1 | 검토 recall | 경계 혼동 |
  |---|---:|---:|---:|
  | multinomial (현행) | 0.6395 | **0.6051** | 127 (32%) |
  | OvR | 0.6336 | 0.5743 | 128 (31%) |
  | OvO | 0.6398 | 0.5705 | 130 (32%) |
  | 캐스케이드 | 0.6159 | 0.5278 | 138 (33%) |

  OvO가 macro F1에서 +0.0003으로 사실상 동률이 됐으나 **검토 recall이 0.605에서 0.571로
  떨어진다.** 경계 혼동은 127~138로 여전히 움직이지 않고 오답의 31~33%를 차지한다.
  데이터가 40% 늘어도 이 비율이 그대로인 것은, 경계가 표본 부족 때문이 아니라는 또 하나의
  증거다.

  경계만 떼서 학습한 오라클 상한은 695건에서 정확도 0.737~0.740이다(다수 클래스 0.521).
  v4의 0.712보다 올랐으나 여전히 상한이며, 캐스케이드가 그 값에 못 미치는 구조도 같다.

- 관측 — **2분류는 v5에서도 큰 폭으로 높다.** `통상수용` 대 `검토필요`로 접으면
  `word+char`가 fold 평균 macro F1 **0.764** / 정확도 0.794다(3분류 0.640). v4에서 관측한
  0.746과 같은 수준이며, 데이터가 늘어도 3분류와의 격차가 유지된다. 경계를 가르지 않기로
  하면 얻는 것이 크다는 사실은 변하지 않았다.

- 남은 것: 파인튜닝과 앙상블이 v4 라벨로 학습된 OOF에 의존한다. v5 기준 최고 구성을
  정하려면 gcube에서 10 fold × 복수 seed를 다시 돌려야 한다. 그때까지 v5의 최고 기록은
  **단독 기준선 0.640**이며, v4의 앙상블 0.677과 직접 비교하지 않는다.

- 산출물: `reports/current/v5/binary_review_results.{md,json}`.
