## 2026-09-07 09:30

- 결정: **문서 정리.** 결정 이력·`NEXT.md`와 요약 문서(`README`·`WORKLOG`·`PIPELINE`·
  `PROJECT_DIRECTION`·`issues/README`) 사이의 불일치를 맞췄다. 연구 결정은 하나도 바꾸지
  않았다. decisions-08이 536줄이라 300줄 규칙에 따라 이 권을 연다.

- 정정 — **decisions-08의 마지막 다섯 항목 헤딩 시각이 커밋 시각보다 1~4시간 늦게 적혀
  있었다.** 시간대로는 설명되지 않는 방향이다. 커밋 시각으로 고치고 본문의 상호 참조
  세 곳도 따라 고쳤다. 내용은 손대지 않았다.

  | 원래 헤딩 | 커밋 시각 (고친 값) |
  |---|---|
  | 08:30 | 07:23 |
  | 09:15 | 07:46 |
  | 10:40 | 07:58 |
  | 11:20 | 08:11 |
  | 12:30 | 08:34 |

- 기록 — **보조 헤드는 이미 v4에서 5회 실행됐고 효과가 없었다.** decisions-08 08:34
  (구 12:30) 항목은 "다음에 구현한다"고 적었으나, `aux-heads` 브랜치에 `MultiHeadModel`·
  `--aux`·`--aux-weight`가 구현돼 있고 결과가 있다. 보조 축은 `blockers`(BCE) +
  `cost_basis` + `build_difficulty` + `domain_dependency`, `[CLS]` 공유.

  | 설정 | +aux | 원본 | 차이 | 경계 혼동 |
  |---|---:|---:|---:|---:|
  | base seed 42 | 0.588 | 0.573 | +0.015 | 118 (원본 111) |
  | base seed 7 | 0.594 | 0.609 | -0.015 | 105 (104) |
  | base seed 13 | 0.589 | 0.597 | -0.008 | 112 (104) |
  | large seed 42 | 0.573 | 0.617 | -0.044 | 113 (107) |
  | large seed 7 | 0.604 | 0.595 | +0.009 | 105 (98) |

  평균 -0.009, 부호가 오가고 전부 측정 하한 0.016 안이며 경계 혼동은 늘었다. 이 사실이
  `NEXT.md`에만 있었으므로 여기로 옮긴다. 출처는 `aux-heads` 브랜치의
  `reports/current/v4/finetune_results.md`. 타깃을 v5/v6 소프트 분포로 바꾸는 변형
  (Fornaciari et al. 2021)만 남았고 우선순위는 낮다.

- 관측 — **`build_label_dataset.py`의 기본 출력이 `label_dataset_v6`이다.** `SOURCE_RUNS`가
  `claude_batches/full_v0.4.0_v6c` 하나이고 `DATASET_VERSION`이 v6이다. README와 PIPELINE의
  "실행 결과에서 v4를 다시 만든다"는 문장은 현재 코드와 맞지 않는다. 코드는 건드리지
  않고 문서에 주의만 달았다. v4·v5를 재생성하려면 두 상수를 되돌려야 한다.

- 조치:
  - `WORKLOG.md` 기준일 09-02 → 09-07, "현재 위치"를 13문서·v5로, 09-04~09-07 요약 추가,
    끝의 "다음에 할 일" 절을 `NEXT.md` 포인터로 대체(미뤄둔 항목 셋은 `NEXT.md`로 이동).
  - `README.md` 첫 문단·라벨 데이터셋 절·환경 변수·노트북 표(13~20)에 v5 반영.
    기존 결과표와 모델 비교 수치는 v4 기준임을 명시하고 그대로 둔다.
  - `PIPELINE.md` 기준일과 흐름도에 v0.4.0·v5를 넣고, 원본 HWP·XLSX 직접 추출 절을 추가.
  - `PROJECT_DIRECTION.md` 범위를 13개로, §3 목록에 신규 3건 추가.
  - `issues/README.md` 첫 줄을 "v3·v4 1,024건 기준, v5 재측정 안 함"으로.
  - `NEXT.md` 브랜치 표기 정정, 보조 헤드 절이 이 항목을 가리키게 함.
  - `reports/current/README.md`에 `v4/`·`v5/`·`claude_batches/`·`solar_runs/` 추가.

- 산출물: 이 항목과 위 문서들. 코드·데이터·보고서는 변경 없음.

## 2026-09-07 14:30

- 결정: **v5 파인튜닝 재학습으로 v5 최고 구성이 정해졌다.** `wc + ftB7 + ftL42` 다수결이
  fold 평균 macro F1 **0.671** / 통합 OOF 0.691이다. 기준선 `word+char` 0.6395 대비
  **+0.031**로 측정 하한 0.016을 넘고, 중첩 선택 13/13이 같은 조합을 골랐다.
  NEXT.md가 적어둔 기대치(~0.67)에 들어맞았다. gcube RTX 4090, 하이퍼파라미터는 v4 실행과 동일.

  | 설정 | v4 fold 평균 | **v5 fold 평균** | v5 통합 OOF | 오답 | 경계 혼동 |
  |---|---:|---:|---:|---:|---:|
  | word+char TF-IDF | 0.614 | 0.640 | 0.654 | 401 | 127 |
  | roberta-base seed 7 (`ftB7`) | 0.609 | 0.634 | 0.661 | 391 | 125 |
  | roberta-large seed 42 (`ftL42`) | 0.617 | **0.655** | 0.677 | 379 | 134 |
  | **wc + ftB7 + ftL42** | 0.643 | **0.671** | **0.691** | **356** | **117** |

- 이유: 데이터 확충(+0.025)과 앙상블(+0.029, v4)이 측정 가능한 유일한 두 레버였고,
  둘을 합치니 그대로 더해졌다(0.614 → 0.671). 오답 겹침은 v4와 같은 구조다 —
  word+char 401 / 파인튜닝 391 중 둘 다 틀린 것 252, 오라클 정확도 0.813.

- 관측 — **large 단독이 기준선보다 +0.016.** v4에서는 +0.003이었다. 하한에 정확히
  걸치고 seed 하나뿐이라 "파인튜닝 단독이 기준선을 넘는다"고는 아직 적지 않는다.
  v4의 large seed 범위가 0.024였다.

- 관측 — **경계 혼동이 127 → 117로 10건 줄었다.** v4 앙상블은 98 → 97이었다. 처음으로
  경계가 움직인 것처럼 보이지만, 앙상블 멤버의 seed에 따라 오가는 폭인지 seed 추가 실행
  뒤에 다시 본다. 오답 감소 45건 중 35건은 여전히 경계 밖이다.

- 조치 — `finetune_ensemble`이 요구하는 v5용 TF-IDF 후보 OOF가 없어서
  `embeddings`(E5 캐시를 `multilingual-e5-small.v5.npz`로 따로 둠) → `candidate_ensemble`
  순으로 만들었다. v5에서도 TF-IDF+E5 결합(0.619)과 soft voting(0.632)은 기준선을 넘지
  않는다. `finetune_ensemble.py`의 보고서 머리말이 924건·10-fold를 하드코딩하고 있어
  실제 평가 건수·문서 수를 쓰도록 고쳤다. v4 보고서는 같은 값으로 재현된다.

- 다음: seed 범위. large seed 7·13, base seed 42·13을 같은 설정으로 돌려 앙상블 점수를
  단일 값이 아니라 범위로 적는다. NEXT.md 1번에 명령을 적었다.

- 산출물: `reports/current/v5/finetune_runs.jsonl`(gcube 커밋 `b7cf282`),
  `reports/current/v5/{finetune_results.md,finetune_results.json,model_candidates.json,model_candidate_oof.csv}`,
  `scripts/evaluation/finetune_ensemble.py` 보고서 머리말 수정.

## 2026-09-07 17:10

- 결정: **v5 앙상블 점수를 범위로 확정한다 — `wc + ftB + ftL` fold 평균 0.666~0.683
  (9쌍 평균 0.6745, sd 0.0050).** 기준선 0.6395 대비 +0.027~+0.044로 9쌍 전부가 측정 하한
  0.016을 넘는다. 사전 등록 조합 `wc+ftB7+ftL42`(0.671)는 그 범위의 아래쪽이었다.
  파인튜닝 단독은 large 3 seed 평균 0.652(+0.012)로 하한 아래라 **기준선을 넘는다고
  적지 않는다.** gcube RTX 4090에서 large seed 7·13, base seed 42·13을 같은 설정으로 추가.

  | 멤버 | seed 42 | seed 7 | seed 13 | 평균 | 폭 |
  |---|---:|---:|---:|---:|---:|
  | roberta-base | 0.640 | 0.634 | 0.643 | 0.639 | 0.009 |
  | roberta-large | 0.655 | 0.648 | 0.652 | 0.652 | 0.008 |
  | 기준선 word+char | | | | 0.640 | |

  | `wc + ftB{s} + ftL{s'}` | ftL42 | ftL7 | ftL13 |
  |---|---:|---:|---:|
  | ftB7 | 0.671 | 0.678 | 0.683 |
  | ftB42 | 0.677 | 0.669 | 0.677 |
  | ftB13 | 0.666 | 0.678 | 0.671 |

- 이유: v4에서 base seed 폭이 0.038이라 단일 값으로는 주장이 안 됐다. v5에서는 seed 폭이
  base 0.009 / large 0.008로 v4의 1/4이다. 데이터가 40% 늘자 학습이 덜 흔들린다.
  base 3 seed 평균은 기준선과 같고(0.639 대 0.640), large는 +0.012로 하한 아래다.
  **파인튜닝 단독으로는 여전히 TF-IDF와 갈리지 않는다**는 v4 결론이 v5에서도 유지된다.
  이득은 오직 섞을 때 나오고, 그 이득은 seed를 바꿔도 사라지지 않는다.

- 관측 — **경계 혼동이 9쌍 전부에서 줄었다.** 기준선 127건이 앙상블에서 111~119건이다.
  v4는 98 → 97로 한 건이었다. 오답 감소(401 → 340~358)의 20~30%가 경계에서 나온다.
  "경계는 어떤 개입으로도 안 움직인다"를 **"경계는 모델 개입으로는 거의 안 움직이고,
  데이터를 늘린 뒤 계열을 섞으면 조금 움직인다"** 로 좁힌다. 여전히 오답의 31~33%다.

- 관측 — **멤버를 더 넣어도 오르지 않는다.** 7개 전부 다수결 0.677, large 3 seed만 0.666,
  5개 0.669~0.670. 3개 조합의 평균 0.6745와 같은 수준이다. v4 09-03의 "모델을 많이 넣을수록
  나빠진다"만큼은 아니지만 이득도 없다. 조합은 `wc + base 1 + large 1` 셋으로 둔다.

- 논문 메모(보관용):
  - 앙상블 점수는 단일 조합이 아니라 **seed 9쌍의 범위**로 보고한다. 사전 등록 조합이
    범위 안에 있었다는 사실이 선택 낙관이 없었다는 근거다.
  - 데이터 증가가 점수뿐 아니라 **seed 분산을 줄인다**(0.038 → 0.009). 학습곡선 논의에
    분산 축을 추가한다.
  - 경계 혼동은 v4에서 "불변", v5에서 "소폭 감소"다. 두 데이터셋에서 같은 방법으로 잰
    값을 나란히 놓고, 감소분이 오답 감소의 일부(20~30%)에 그친다는 것까지 적는다.

- 정리 — 컨테이너의 `runs.log`(학습 로그 1,391줄)가 `2b162fe`로 커밋돼 있었다. 저장소에서
  빼고 `.gitignore`에 넣었다. 이력에는 남는다.

- 산출물: `reports/current/v5/finetune_runs.jsonl`(gcube 커밋 `962e5e3`), 9쌍 표는 이 항목이
  기록이며 `finetune_ensemble.load_members`·`vote`·`describe`로 재현된다.
  `reports/current/v5/finetune_results.{md,json}`은 멤버 10종으로 재생성.

## 2026-09-07 18:40

- 결정: **조항 종류 군집의 문서 확산도 특징을 채택하지 않는다.** 2026-09-06 18:08이
  "아직 시험하지 않았다"고 남긴 빈칸을 채웠다. E5 임베딩 KMeans(k=40·80·160)로 군집을 만들고
  군집별 "학습 문서 11개 중 몇 개에 걸쳐 있나"(0~1)를 `word+char` TF-IDF에 특징 하나로
  붙였다. 여섯 설정 전부 기준선 아래이고 우세 최대 6/13이다.

  | 설정 | fold 평균 | 차이 | 우세 | 검토 recall |
  |---|---:|---:|---:|---:|
  | 기준선 word+char | 0.6395 | — | — | 0.605 |
  | k=40, w=1 | 0.6227 | -0.017 | 3/13 | 0.585 |
  | k=80, w=1 | 0.6308 | -0.009 | 6/13 | 0.576 |
  | k=160, w=1 | 0.6347 | -0.005 | 5/13 | 0.600 |
  | (w=3은 각각 -0.018 / -0.011 / -0.005) | | | | |

- 이유: **신호는 있지만 약하고 한쪽으로만 있다.** 전체 1,445건에서 확산도 상위 4분위
  (563건)의 통상수용 비율은 63.4%로 나머지(39~51%)보다 높다. "어느 RFP에나 있는 조항은
  기본 수행 범위"라는 실무 관측이 숫자로 보이기는 한다. 그런데 그 아래 세 구간은 단조가
  아니고(49.6 → 50.7 → 38.6%), 특징을 넣으면 검토 recall이 0.605 → 0.58로 내려간다.
  통상수용을 조금 더 맞히고 소수 클래스를 내주는 교환이라 macro F1에서 손해다.
  k가 커질수록 손해가 줄어드는 것은 군집이 잘게 쪼개져 특징이 무의미해지는 방향이지
  신호가 살아나는 방향이 아니다.

  군집 생성과 확산도 집계는 fold마다 학습 문서 11개로만 했고, 평가 문서는 최근접 군집에
  배정만 받았다. 누수는 없다.

- 관측 — 이로써 "조항의 보편성"을 재는 세 눈금(문자 유사도 · 유형 12종 · 임베딩 군집)이
  모두 분류 특징으로는 실패했다. 임베딩 계열은 v5에서 E5 단독 0.559, TF-IDF 결합 0.619,
  앙상블 멤버로도 wc보다 낮았으며, 이 실험이 임베딩으로 시도할 마지막 형태였다.
  **임베딩 축은 닫는다.**

- 논문 메모(보관용): 확산도 상위 4분위의 63.4%는 "관행 조항"이 실재한다는 근거로 쓸 수
  있다. 다만 분류 특징으로는 손해였다는 것을 함께 적는다 — 신호가 있는 것과 선형 모델이
  그 신호를 쓸 수 있는 것은 다르다.

- 산출물: `scripts/evaluation/cluster_universality.py`,
  `reports/current/v5/cluster_universality.json`.

## 2026-09-07 19:20

- 관측: **모델이 틀리는 자리는 "뜻이 같은 조항이 다른 문서에서 다른 라벨을 받은 자리"다.**
  E5 임베딩으로 각 요구사항의 **다른 문서** 최근접 5건을 찾아 그 다수결 라벨이 자기
  라벨과 맞는 비율을 쟀다(학습 없음, 문서 간 kNN). 모델 정오답별로 구간이 겹치지 않는다.

  | 집단 | 건수 | 이웃 라벨 일치 | Wilson 95% |
  |---|---:|---:|---|
  | 모델이 맞힌 건 | 944 | **78.9%** | 76.2~81.4 |
  | 경계 밖 오답 | 274 | 30.3% | 25.2~36.0 |
  | **경계 혼동** | **127** | **26.0%** | **19.1~34.2** |
  | v5·v6 일치 | 1,075 | 67.8% | 65.0~70.5 |
  | v5·v6 불일치 | 370 | 54.1% | 49.0~59.1 |
  | 라벨=통상수용 | 750 | 75.3% | |
  | 라벨=견적반영 | 333 | 53.5% | |
  | 라벨=계약·질의검토 | 362 | 51.4% | |

  경계 혼동 127건은 이웃 일치가 26%로, 세 라벨 우연 수준 근처다. **비슷한 문장이 다른
  RFP에서는 대부분 다른 라벨을 받았다는 뜻**이고, 그 차이는 문장이 아니라 문서 맥락(발주
  기관·사업 성격·계약 조건)에서 온다. 2026-09-02의 28건 정독이 낸 결론 "판정에 문서 밖
  지식이 필요하다"를 전수에서 수치로 확인한 것이다.

  이 측정은 **v6와 무관**하다. 2026-09-07 08:34이 노트북 20의 47.2%에 순환성이 있다고
  정정했는데(v6 프롬프트가 경계 정의를 직접 바꿈), 여기서는 라벨 하나(v5)와 임베딩만
  쓰므로 그 문제가 없다. "정답이 흔들린다"의 독립 증거로 이쪽을 쓴다.

  참고로 이 다수결을 그대로 예측으로 쓰면 macro F1 0.598이다. 학습 없이 "다른 문서에서
  비슷한 조항이 받은 라벨"만으로 기준선(0.640)의 93%에 닿는다.

- 관측 — **아웃라이어는 오답이 아니다.** 최근접 유사도 하위 5%(73건)의 모델 오답률은
  30.9%로 나머지(29.8%)와 같다. 추출 결함도 아니다 — 한글 비율 중앙값이 0.65 대 0.66으로
  같고, 원문이 짧을 뿐이다(중앙값 136자 대 356자). 내용은 `백업SW 250TB`, `DISA 표준어댑터`,
  `SAP ABAP 개발자`처럼 **한 사업에만 있는 구체 항목**이다. 문서별로는 mfds 15, 대법원 13,
  국방 11, 지역난방 11로 도메인이 특수한 문서에 몰린다. 검수 대상으로는 쓸 수 있으나
  모델 오류의 원인은 아니다.

- 관측 — **앵커 100건은 군집 80개 중 48개만 덮는다.** 앵커 없는 군집의 요구사항이 413건
  (28.6%)이고, 성능 시험(23건)·인력 교체와 조직 구성(23건)·데이터 파이프라인(39건) 같은
  덩어리째 비어 있다. issues/008의 "인출 유사도 중앙값 0.090"은 이 빈 곳에서 나온다.
  앵커 풀은 동결이라 고치지 않으며, 재설계 시 층화 축을 문서×카테고리가 아니라 **군집**으로
  잡으라는 기록으로 남긴다. 군집 안 다수 라벨 점유율이 60% 미만인 군집의 요구사항은
  437건(30.2%)이다.

- 논문 메모(보관용):
  - 오류 분석의 세 축이 모였다 — 모델 정오답(9/2), 라벨 불안정(9/7 07:23, v6 순환성
    있음), **문서 간 이웃 라벨 불일치(오늘, 독립)**. 경계 혼동 127건은 세 축 모두에서
    같은 자리다. 이 표를 "왜 3분류는 0.64에서 멈추는가"의 근거로 쓴다.
  - kNN 0.598은 "학습 없이 검색만으로 얼마나 가는가"의 하한이다. 파인튜닝·TF-IDF가
    그 위에 얹는 것이 0.04~0.08뿐이라는 것을 함께 적으면 과제의 성격이 드러난다.
  - 앵커 풀 커버리지 48/80은 few-shot 설계의 한계 사례다. 라벨 균형(1:1:1)으로 뽑으면
    의미 공간의 40%가 비는데도 그것을 알 수 없었다.

- 산출물: `scripts/evaluation/cluster_diagnostics.py`,
  `reports/current/v5/cluster_diagnostics.json`.
