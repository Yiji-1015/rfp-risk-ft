## 2026-09-07 09:30

- 결정: **문서 정리.** 결정 이력·`NEXT.md`와 요약 문서(`README`·`WORKLOG`·`PIPELINE`·
  `PROJECT_DIRECTION`·`issues/README`) 사이의 불일치를 맞췄다. 연구 결정은 하나도 바꾸지
  않았다. decisions-08이 536줄이라 300줄 규칙에 따라 이 권을 연다.

- 정정 — **decisions-08의 마지막 다섯 항목 헤딩 시각이 커밋 시각보다 1~4시간 늦게 적혀
  있었다.** 시간대로는 설명되지 않는 방향이다. 커밋 시각으로 고치고 본문의 상호 참조
  세 곳도 따라 고쳤다. 내용은 손대지 않았다.

  | 원래 헤딩 | 커밋 시각 (고친 값) |
  |---|---|
  | 08:30 | 07:23 |
  | 09:15 | 07:46 |
  | 10:40 | 07:58 |
  | 11:20 | 08:11 |
  | 12:30 | 08:34 |

- 기록 — **보조 헤드는 이미 v4에서 5회 실행됐고 효과가 없었다.** decisions-08 08:34
  (구 12:30) 항목은 "다음에 구현한다"고 적었으나, `aux-heads` 브랜치에 `MultiHeadModel`·
  `--aux`·`--aux-weight`가 구현돼 있고 결과가 있다. 보조 축은 `blockers`(BCE) +
  `cost_basis` + `build_difficulty` + `domain_dependency`, `[CLS]` 공유.

  | 설정 | +aux | 원본 | 차이 | 경계 혼동 |
  |---|---:|---:|---:|---:|
  | base seed 42 | 0.588 | 0.573 | +0.015 | 118 (원본 111) |
  | base seed 7 | 0.594 | 0.609 | -0.015 | 105 (104) |
  | base seed 13 | 0.589 | 0.597 | -0.008 | 112 (104) |
  | large seed 42 | 0.573 | 0.617 | -0.044 | 113 (107) |
  | large seed 7 | 0.604 | 0.595 | +0.009 | 105 (98) |

  평균 -0.009, 부호가 오가고 전부 측정 하한 0.016 안이며 경계 혼동은 늘었다. 이 사실이
  `NEXT.md`에만 있었으므로 여기로 옮긴다. 출처는 `aux-heads` 브랜치의
  `reports/current/v4/finetune_results.md`. 타깃을 v5/v6 소프트 분포로 바꾸는 변형
  (Fornaciari et al. 2021)만 남았고 우선순위는 낮다.

- 관측 — **`build_label_dataset.py`의 기본 출력이 `label_dataset_v6`이다.** `SOURCE_RUNS`가
  `claude_batches/full_v0.4.0_v6c` 하나이고 `DATASET_VERSION`이 v6이다. README와 PIPELINE의
  "실행 결과에서 v4를 다시 만든다"는 문장은 현재 코드와 맞지 않는다. 코드는 건드리지
  않고 문서에 주의만 달았다. v4·v5를 재생성하려면 두 상수를 되돌려야 한다.

- 조치:
  - `WORKLOG.md` 기준일 09-02 → 09-07, "현재 위치"를 13문서·v5로, 09-04~09-07 요약 추가,
    끝의 "다음에 할 일" 절을 `NEXT.md` 포인터로 대체(미뤄둔 항목 셋은 `NEXT.md`로 이동).
  - `README.md` 첫 문단·라벨 데이터셋 절·환경 변수·노트북 표(13~20)에 v5 반영.
    기존 결과표와 모델 비교 수치는 v4 기준임을 명시하고 그대로 둔다.
  - `PIPELINE.md` 기준일과 흐름도에 v0.4.0·v5를 넣고, 원본 HWP·XLSX 직접 추출 절을 추가.
  - `PROJECT_DIRECTION.md` 범위를 13개로, §3 목록에 신규 3건 추가.
  - `issues/README.md` 첫 줄을 "v3·v4 1,024건 기준, v5 재측정 안 함"으로.
  - `NEXT.md` 브랜치 표기 정정, 보조 헤드 절이 이 항목을 가리키게 함.
  - `reports/current/README.md`에 `v4/`·`v5/`·`claude_batches/`·`solar_runs/` 추가.

- 산출물: 이 항목과 위 문서들. 코드·데이터·보고서는 변경 없음.

## 2026-09-07 14:30

- 결정: **v5 파인튜닝 재학습으로 v5 최고 구성이 정해졌다.** `wc + ftB7 + ftL42` 다수결이
  fold 평균 macro F1 **0.671** / 통합 OOF 0.691이다. 기준선 `word+char` 0.6395 대비
  **+0.031**로 측정 하한 0.016을 넘고, 중첩 선택 13/13이 같은 조합을 골랐다.
  NEXT.md가 적어둔 기대치(~0.67)에 들어맞았다. gcube RTX 4090, 하이퍼파라미터는 v4 실행과 동일.

  | 설정 | v4 fold 평균 | **v5 fold 평균** | v5 통합 OOF | 오답 | 경계 혼동 |
  |---|---:|---:|---:|---:|---:|
  | word+char TF-IDF | 0.614 | 0.640 | 0.654 | 401 | 127 |
  | roberta-base seed 7 (`ftB7`) | 0.609 | 0.634 | 0.661 | 391 | 125 |
  | roberta-large seed 42 (`ftL42`) | 0.617 | **0.655** | 0.677 | 379 | 134 |
  | **wc + ftB7 + ftL42** | 0.643 | **0.671** | **0.691** | **356** | **117** |

- 이유: 데이터 확충(+0.025)과 앙상블(+0.029, v4)이 측정 가능한 유일한 두 레버였고,
  둘을 합치니 그대로 더해졌다(0.614 → 0.671). 오답 겹침은 v4와 같은 구조다 —
  word+char 401 / 파인튜닝 391 중 둘 다 틀린 것 252, 오라클 정확도 0.813.

- 관측 — **large 단독이 기준선보다 +0.016.** v4에서는 +0.003이었다. 하한에 정확히
  걸치고 seed 하나뿐이라 "파인튜닝 단독이 기준선을 넘는다"고는 아직 적지 않는다.
  v4의 large seed 범위가 0.024였다.

- 관측 — **경계 혼동이 127 → 117로 10건 줄었다.** v4 앙상블은 98 → 97이었다. 처음으로
  경계가 움직인 것처럼 보이지만, 앙상블 멤버의 seed에 따라 오가는 폭인지 seed 추가 실행
  뒤에 다시 본다. 오답 감소 45건 중 35건은 여전히 경계 밖이다.

- 조치 — `finetune_ensemble`이 요구하는 v5용 TF-IDF 후보 OOF가 없어서
  `embeddings`(E5 캐시를 `multilingual-e5-small.v5.npz`로 따로 둠) → `candidate_ensemble`
  순으로 만들었다. v5에서도 TF-IDF+E5 결합(0.619)과 soft voting(0.632)은 기준선을 넘지
  않는다. `finetune_ensemble.py`의 보고서 머리말이 924건·10-fold를 하드코딩하고 있어
  실제 평가 건수·문서 수를 쓰도록 고쳤다. v4 보고서는 같은 값으로 재현된다.

- 다음: seed 범위. large seed 7·13, base seed 42·13을 같은 설정으로 돌려 앙상블 점수를
  단일 값이 아니라 범위로 적는다. NEXT.md 1번에 명령을 적었다.

- 산출물: `reports/current/v5/finetune_runs.jsonl`(gcube 커밋 `b7cf282`),
  `reports/current/v5/{finetune_results.md,finetune_results.json,model_candidates.json,model_candidate_oof.csv}`,
  `scripts/evaluation/finetune_ensemble.py` 보고서 머리말 수정.

## 2026-09-07 17:10

- 결정: **v5 앙상블 점수를 범위로 확정한다 — `wc + ftB + ftL` fold 평균 0.666~0.683
  (9쌍 평균 0.6745, sd 0.0050).** 기준선 0.6395 대비 +0.027~+0.044로 9쌍 전부가 측정 하한
  0.016을 넘는다. 사전 등록 조합 `wc+ftB7+ftL42`(0.671)는 그 범위의 아래쪽이었다.
  파인튜닝 단독은 large 3 seed 평균 0.652(+0.012)로 하한 아래라 **기준선을 넘는다고
  적지 않는다.** gcube RTX 4090에서 large seed 7·13, base seed 42·13을 같은 설정으로 추가.

  | 멤버 | seed 42 | seed 7 | seed 13 | 평균 | 폭 |
  |---|---:|---:|---:|---:|---:|
  | roberta-base | 0.640 | 0.634 | 0.643 | 0.639 | 0.009 |
  | roberta-large | 0.655 | 0.648 | 0.652 | 0.652 | 0.008 |
  | 기준선 word+char | | | | 0.640 | |

  | `wc + ftB{s} + ftL{s'}` | ftL42 | ftL7 | ftL13 |
  |---|---:|---:|---:|
  | ftB7 | 0.671 | 0.678 | 0.683 |
  | ftB42 | 0.677 | 0.669 | 0.677 |
  | ftB13 | 0.666 | 0.678 | 0.671 |

- 이유: v4에서 base seed 폭이 0.038이라 단일 값으로는 주장이 안 됐다. v5에서는 seed 폭이
  base 0.009 / large 0.008로 v4의 1/4이다. 데이터가 40% 늘자 학습이 덜 흔들린다.
  base 3 seed 평균은 기준선과 같고(0.639 대 0.640), large는 +0.012로 하한 아래다.
  **파인튜닝 단독으로는 여전히 TF-IDF와 갈리지 않는다**는 v4 결론이 v5에서도 유지된다.
  이득은 오직 섞을 때 나오고, 그 이득은 seed를 바꿔도 사라지지 않는다.

- 관측 — **경계 혼동이 9쌍 전부에서 줄었다.** 기준선 127건이 앙상블에서 111~119건이다.
  v4는 98 → 97로 한 건이었다. 오답 감소(401 → 340~358)의 20~30%가 경계에서 나온다.
  "경계는 어떤 개입으로도 안 움직인다"를 **"경계는 모델 개입으로는 거의 안 움직이고,
  데이터를 늘린 뒤 계열을 섞으면 조금 움직인다"** 로 좁힌다. 여전히 오답의 31~33%다.

- 관측 — **멤버를 더 넣어도 오르지 않는다.** 7개 전부 다수결 0.677, large 3 seed만 0.666,
  5개 0.669~0.670. 3개 조합의 평균 0.6745와 같은 수준이다. v4 09-03의 "모델을 많이 넣을수록
  나빠진다"만큼은 아니지만 이득도 없다. 조합은 `wc + base 1 + large 1` 셋으로 둔다.

- 논문 메모(보관용):
  - 앙상블 점수는 단일 조합이 아니라 **seed 9쌍의 범위**로 보고한다. 사전 등록 조합이
    범위 안에 있었다는 사실이 선택 낙관이 없었다는 근거다.
  - 데이터 증가가 점수뿐 아니라 **seed 분산을 줄인다**(0.038 → 0.009). 학습곡선 논의에
    분산 축을 추가한다.
  - 경계 혼동은 v4에서 "불변", v5에서 "소폭 감소"다. 두 데이터셋에서 같은 방법으로 잰
    값을 나란히 놓고, 감소분이 오답 감소의 일부(20~30%)에 그친다는 것까지 적는다.

- 정리 — 컨테이너의 `runs.log`(학습 로그 1,391줄)가 `2b162fe`로 커밋돼 있었다. 저장소에서
  빼고 `.gitignore`에 넣었다. 이력에는 남는다.

- 산출물: `reports/current/v5/finetune_runs.jsonl`(gcube 커밋 `962e5e3`), 9쌍 표는 이 항목이
  기록이며 `finetune_ensemble.load_members`·`vote`·`describe`로 재현된다.
  `reports/current/v5/finetune_results.{md,json}`은 멤버 10종으로 재생성.

## 2026-09-07 18:40

- 결정: **조항 종류 군집의 문서 확산도 특징을 채택하지 않는다.** 2026-09-06 18:08이
  "아직 시험하지 않았다"고 남긴 빈칸을 채웠다. E5 임베딩 KMeans(k=40·80·160)로 군집을 만들고
  군집별 "학습 문서 11개 중 몇 개에 걸쳐 있나"(0~1)를 `word+char` TF-IDF에 특징 하나로
  붙였다. 여섯 설정 전부 기준선 아래이고 우세 최대 6/13이다.

  | 설정 | fold 평균 | 차이 | 우세 | 검토 recall |
  |---|---:|---:|---:|---:|
  | 기준선 word+char | 0.6395 | — | — | 0.605 |
  | k=40, w=1 | 0.6227 | -0.017 | 3/13 | 0.585 |
  | k=80, w=1 | 0.6308 | -0.009 | 6/13 | 0.576 |
  | k=160, w=1 | 0.6347 | -0.005 | 5/13 | 0.600 |
  | (w=3은 각각 -0.018 / -0.011 / -0.005) | | | | |

- 이유: **신호는 있지만 약하고 한쪽으로만 있다.** 전체 1,445건에서 확산도 상위 4분위
  (563건)의 통상수용 비율은 63.4%로 나머지(39~51%)보다 높다. "어느 RFP에나 있는 조항은
  기본 수행 범위"라는 실무 관측이 숫자로 보이기는 한다. 그런데 그 아래 세 구간은 단조가
  아니고(49.6 → 50.7 → 38.6%), 특징을 넣으면 검토 recall이 0.605 → 0.58로 내려간다.
  통상수용을 조금 더 맞히고 소수 클래스를 내주는 교환이라 macro F1에서 손해다.
  k가 커질수록 손해가 줄어드는 것은 군집이 잘게 쪼개져 특징이 무의미해지는 방향이지
  신호가 살아나는 방향이 아니다.

  군집 생성과 확산도 집계는 fold마다 학습 문서 11개로만 했고, 평가 문서는 최근접 군집에
  배정만 받았다. 누수는 없다.

- 관측 — 이로써 "조항의 보편성"을 재는 세 눈금(문자 유사도 · 유형 12종 · 임베딩 군집)이
  모두 분류 특징으로는 실패했다. 임베딩 계열은 v5에서 E5 단독 0.559, TF-IDF 결합 0.619,
  앙상블 멤버로도 wc보다 낮았으며, 이 실험이 임베딩으로 시도할 마지막 형태였다.
  **임베딩 축은 닫는다.**

- 논문 메모(보관용): 확산도 상위 4분위의 63.4%는 "관행 조항"이 실재한다는 근거로 쓸 수
  있다. 다만 분류 특징으로는 손해였다는 것을 함께 적는다 — 신호가 있는 것과 선형 모델이
  그 신호를 쓸 수 있는 것은 다르다.

- 산출물: `scripts/evaluation/cluster_universality.py`,
  `reports/current/v5/cluster_universality.json`.

## 2026-09-07 19:20

- 관측: **모델이 틀리는 자리는 "뜻이 같은 조항이 다른 문서에서 다른 라벨을 받은 자리"다.**
  E5 임베딩으로 각 요구사항의 **다른 문서** 최근접 5건을 찾아 그 다수결 라벨이 자기
  라벨과 맞는 비율을 쟀다(학습 없음, 문서 간 kNN). 모델 정오답별로 구간이 겹치지 않는다.

  | 집단 | 건수 | 이웃 라벨 일치 | Wilson 95% |
  |---|---:|---:|---|
  | 모델이 맞힌 건 | 944 | **78.9%** | 76.2~81.4 |
  | 경계 밖 오답 | 274 | 30.3% | 25.2~36.0 |
  | **경계 혼동** | **127** | **26.0%** | **19.1~34.2** |
  | v5·v6 일치 | 1,075 | 67.8% | 65.0~70.5 |
  | v5·v6 불일치 | 370 | 54.1% | 49.0~59.1 |
  | 라벨=통상수용 | 750 | 75.3% | |
  | 라벨=견적반영 | 333 | 53.5% | |
  | 라벨=계약·질의검토 | 362 | 51.4% | |

  경계 혼동 127건은 이웃 일치가 26%로, 세 라벨 우연 수준 근처다. **비슷한 문장이 다른
  RFP에서는 대부분 다른 라벨을 받았다는 뜻**이고, 그 차이는 문장이 아니라 문서 맥락(발주
  기관·사업 성격·계약 조건)에서 온다. 2026-09-02의 28건 정독이 낸 결론 "판정에 문서 밖
  지식이 필요하다"를 전수에서 수치로 확인한 것이다.

  이 측정은 **v6와 무관**하다. 2026-09-07 08:34이 노트북 20의 47.2%에 순환성이 있다고
  정정했는데(v6 프롬프트가 경계 정의를 직접 바꿈), 여기서는 라벨 하나(v5)와 임베딩만
  쓰므로 그 문제가 없다. "정답이 흔들린다"의 독립 증거로 이쪽을 쓴다.

  참고로 이 다수결을 그대로 예측으로 쓰면 macro F1 0.598이다. 학습 없이 "다른 문서에서
  비슷한 조항이 받은 라벨"만으로 기준선(0.640)의 93%에 닿는다.

- 관측 — **아웃라이어는 오답이 아니다.** 최근접 유사도 하위 5%(73건)의 모델 오답률은
  30.9%로 나머지(29.8%)와 같다. 추출 결함도 아니다 — 한글 비율 중앙값이 0.65 대 0.66으로
  같고, 원문이 짧을 뿐이다(중앙값 136자 대 356자). 내용은 `백업SW 250TB`, `DISA 표준어댑터`,
  `SAP ABAP 개발자`처럼 **한 사업에만 있는 구체 항목**이다. 문서별로는 mfds 15, 대법원 13,
  국방 11, 지역난방 11로 도메인이 특수한 문서에 몰린다. 검수 대상으로는 쓸 수 있으나
  모델 오류의 원인은 아니다.

- 관측 — **앵커 100건은 군집 80개 중 48개만 덮는다.** 앵커 없는 군집의 요구사항이 413건
  (28.6%)이고, 성능 시험(23건)·인력 교체와 조직 구성(23건)·데이터 파이프라인(39건) 같은
  덩어리째 비어 있다. issues/008의 "인출 유사도 중앙값 0.090"은 이 빈 곳에서 나온다.
  앵커 풀은 동결이라 고치지 않으며, 재설계 시 층화 축을 문서×카테고리가 아니라 **군집**으로
  잡으라는 기록으로 남긴다. 군집 안 다수 라벨 점유율이 60% 미만인 군집의 요구사항은
  437건(30.2%)이다.

- 논문 메모(보관용):
  - 오류 분석의 세 축이 모였다 — 모델 정오답(9/2), 라벨 불안정(9/7 07:23, v6 순환성
    있음), **문서 간 이웃 라벨 불일치(오늘, 독립)**. 경계 혼동 127건은 세 축 모두에서
    같은 자리다. 이 표를 "왜 3분류는 0.64에서 멈추는가"의 근거로 쓴다.
  - kNN 0.598은 "학습 없이 검색만으로 얼마나 가는가"의 하한이다. 파인튜닝·TF-IDF가
    그 위에 얹는 것이 0.04~0.08뿐이라는 것을 함께 적으면 과제의 성격이 드러난다.
  - 앵커 풀 커버리지 48/80은 few-shot 설계의 한계 사례다. 라벨 균형(1:1:1)으로 뽑으면
    의미 공간의 40%가 비는데도 그것을 알 수 없었다.

- 산출물: `scripts/evaluation/cluster_diagnostics.py`,
  `reports/current/v5/cluster_diagnostics.json`.

## 2026-09-07 19:50

- 관측: **19:20의 이웃 라벨 불일치는 임베딩 선택에 기대지 않는다.** 같은 측정을 기준선과
  같은 `word+char` TF-IDF 코사인 유사도로 다시 했다(벡터라이저는 전체에 비지도로 맞춤,
  라벨 안 씀). 두 표현이 고른 이웃 5건은 평균 37.9%만 겹치는데도 집단별 비율이 같다.

  | 집단 | E5 이웃 | TF-IDF 이웃 |
  |---|---:|---:|
  | 모델이 맞힌 건 (944) | 78.9% | 82.8% |
  | 경계 밖 오답 (274) | 30.3% | 29.9% |
  | **경계 혼동 (127)** | **26.0%** | **22.8%** |
  | kNN 다수결 macro F1 | 0.598 | 0.595 |

  서로 다른 이웃을 골라도 "경계 혼동은 이웃 라벨이 갈리는 자리"가 그대로 나온다.
  표현이 아니라 데이터의 성질이다. TF-IDF 쪽은 모델과 같은 공간이라 완전히 독립은
  아니지만, E5 쪽이 독립이고 둘이 일치하므로 어느 한쪽의 우연이 아니다.

- 논문 메모(보관용): 이 표를 19:20 표와 합쳐 두 열로 보고한다. 이웃 겹침 37.9%를 함께
  적어야 "같은 이웃을 두 번 센 것"이라는 반론을 막는다.

- 산출물: `scripts/evaluation/cluster_diagnostics.py`에 TF-IDF 대조군 추가,
  `reports/current/v5/cluster_diagnostics.json` 재생성.

## 2026-09-07 21:10

- 결정: **데이콘 「나라장터 자체입찰 공고 법령 위반사항 모니터링 AI 경진대회」에 이 저장소의
  라벨링 파이프라인과 교훈을 가져가 참여를 검토한다.** 모델은 못 가져간다 — 규칙이 참가자가
  학습시킨 분류기(TF-IDF·파인튜닝 포함)를 판정에 쓰지 못하게 한다. 이 저장소의 연구
  결정은 바꾸지 않는다.
  (https://www.dacon.io/competitions/official/236754, 리더보드 마감 2026-09-29)

- 대회 구조: 입찰공고 1건(공고문·첨부 텍스트 + 나라장터 meta 21필드) → 24개 검토 항목의
  위반 여부(0/1)와 근거 문구(본문 부분문자열 500자 이하). 부재탐지형 5개(10·11·16·18·20)는
  근거 공란. 고정 LLM `google/gemma-4-26B-A4B-it`(파인튜닝 금지) + `BAAI/bge-m3`. 무라벨
  2만 건, 라벨 예시 200건. **자가 라벨링에만 외부 LLM(Claude 포함) 허용.** 하루 1회 제출.

- 가져갈 것 (이 저장소에서 검증된 순서):
  1. `run_claude_batch.py` — 배치 API, manifest에 프롬프트 sha256·모델, degeneration 재시도,
     조건 다르면 이어쓰기 거부. 24항목 스키마로 프롬프트만 바꾼다.
  2. 3회 반복 일치 스크리닝(결정 22·25) — 사례집에 넣을 자가 라벨의 품질 필터.
  3. **앵커는 라벨이 아니라 군집으로 층화한다**(19:20) — 라벨 1:1:1 앵커가 의미 공간의
     40%를 비웠던 실수를 bge-m3 군집 층화로 피한다.
  4. 근거 인용 후처리 — v6c에서 인용률 81%, 길이 제한과 충돌(03:17). 추론 뒤 부분문자열
     검증과 원문 재탐색이 필수.
  5. 프롬프트 한 줄이 라벨 분포를 옮긴다(v6b, 127건) — 항목별 양성 비율을 매번 재고,
     지시 이행률(규칙 불일치·인용률)을 일치율과 따로 본다(Solar 기각 근거).
  6. 측정 하한 — dev 200건은 항목당 양성 5~8건이라 F1이 한 건에 0.1씩 흔들린다.
     그 아래 차이로 프롬프트를 고르지 않는다.

- 새로 할 것: meta 21필드로 결정되는 항목(낙찰하한율·지역제한·긴급공고·게시일-개찰일
  간격·중소기업 경쟁제품 지정 csv)은 `derive_primary_action`식 고정 규칙으로 먼저 푼다.
  부재탐지형은 법령패키지 조문을 RAG로 붙여 "이 조항이 있는가"로 바꿔 묻는다.
  첫 작업은 `항목표.json`을 열어 24개를 규칙형·LLM형·부재형으로 나누는 것이다.

- 비용 주의: 공고문은 요구사항 한 건보다 훨씬 길어 건당 8원이 아니다. 전수 2만이 아니라
  1~2천 건부터. 큰 배치는 캐시 생성이 동시에 일어나 견적의 2배가 나온 적이 있다(03:17).

- 산출물: 없음(검토). 실행 계획은 `NEXT.md` 5번.

## 2026-09-07 18:08

- 결정: **Solar Pro 4를 이견 사례에 붙이는 현재 설정의 확대를 보류한다.** 사용자가 승인한
  100건 파일럿을 실행했다. 기존 앙상블 `wc+ftB7+ftL42`의 이견 435건 중 문서×표결 유형에
  비례한 100건(seed 42, 13문서)을 정답을 보지 않고 고정 추출했다. 동결 v5 라벨은 유지한다.
- 조건: `solar-pro4-260806`, temperature 0, reasoning_effort low, max_tokens 4096.
  v5 판단 기준에 원문 인용 형식만 덧붙인 zero-shot이다. 입력에는 UID·요구사항명·원문만
  제공하고 평가 정답·기존 해설·앙상블 예측을 제외했다. 유효 응답은 고정
  `derive_primary_action`을 적용하고 실패는 기존 앙상블을 유지하는 규칙을 실행 전에 정했다.
- 결과: 100건 중 정상 82건, 실패 18건(설명 300자 초과 12, 출력 토큰 한도 4, 연결 오류 2).
  오답 16건을 고쳤지만 정답 17건을 악화시켰다. 유효 응답의 주 라벨 규칙 불일치는 0건,
  원문과 일치하는 인용은 50/82건이다.
  - 표본 100건 통합 macro F1: 0.5877 → 0.5653, 오답 40 → 41.
  - **100건만 교체한** 전체 1,345건 fold 평균: 0.6714 → 0.6731 (+0.0017).
    통합 macro F1: 0.6913 → 0.6889, 오답 356 → 357. 미호출 이견 335건의 개선을 외삽하지 않는다.
  - 사후 진단: 추가 API 없이 설명 길이 제한만 풀면 12건을 더 검증할 수 있으나 전체 fold
    평균 0.6709, 통합 0.6868, 오답 359건이다. 형식 오류만 고치면 점수가 오른다는 근거도 없다.
    이 수치는 사전 고정 결과를 대체하지 않는다.
- 실행 기록: 정상·실패를 건별 저장하고 재개 시 이미 기록된 UID는 다시 호출하지 않았다.
  최초 샌드박스 연결 실패는 별도 보존 후 네트워크 허용 환경에서 실행했다. 대기 시간을 줄이려고
  동시성 1 → 2, 2건 묶음 → 빈 자리부터 채우는 방식으로 바꿨다. 중단 당시 최대 3건의 요청은
  토큰 사용량을 확인할 수 없어 별도 기록했다. 기록된 응답 토큰 기준 비용 약 $0.02263(세금,
  중단·재시도 미기록 사용량 제외), 단가는 실행일 Upstage 공식 할인 가격이다.
- 이유: 주 지표의 변화는 작고 다른 집계에서는 악화됐다. 현재 설정을 435건 전체로 확대할
  근거가 약하다. 이는 이 설정의 결과이며 Upstage 전체의 성능 결론으로 일반화하지 않는다.
- 논문 메모: 기존 LLM 생성 v5 라벨의 재현도를 보는 탐색적 보조 추론 실험이다. 소형 모델
  단독 성능이나 실무 정답률이 아니다. v5 라벨 생성은 few-shot, 이번 Solar 추론은 zero-shot이므로
  모델만 교체한 통제 비교가 아니다. 확정 효과는 설계에 쓰지 않은 새 RFP에서 확인해야 한다.
- 산출물: `scripts/evaluation/solar_disagreement_pilot.py`, `tests/test_solar_disagreement_pilot.py`,
  `reports/current/solar_runs/disagreement_v5_100_s42/`의 조건·입력·응답·비교 보고서.
  실행 명령은 README에 있다. 관련 검사 27개 통과, 별도 계산으로 UID 100건·전체 fold 점수를 대조했다.

## 2026-09-07 23:20

- 결정: **경계 이견에 문서 맥락을 주는 재판정 실험을 준비하고 실행은 보류한다**(브랜치
  `boundary-context`). 19:20 진단이 "경계는 문서 맥락이 가른다"였고, Solar 파일럿(18:08)이
  "원문만 다시 읽히면 16 고치고 17 망친다"는 대조군을 남겼다. 같은 100건을 Claude Sonnet 5로
  `plain`(원문만) / `context`(원문 + 문서 맥락 카드) 두 팔 zero-shot 판정해 맥락의 효과만 분리한다.
- 문서 맥락 카드 13장을 RFP 원본에서 만들었다(`data/context/document_context_v1.jsonl`):
  발주기관·사업비·기간·망 환경·인프라·조달 조건·도메인·과업 요약. 원문에 없는 항목은
  미기재로 두고 추측하지 않았다. 예산 미기재 4건(강원랜드·남동·국방·인천공항), 지역난방은
  요구사항 정의서만 있어 예산·기간 미확인.
- zero-shot으로 두는 이유: v5 정답은 층화 퓨샷으로 만들어졌으므로 같은 조건으로 다시 돌리면
  정답을 재생성하는 것이 되어 "고침"이 의미를 잃는다. 대신 v5 판단 규칙 1(문서에 없는
  예산·기간을 추측하지 않는다)이 정답 생성 시 맥락을 배제했으므로, context 팔이 v5와 멀어지는
  것을 오류로만 읽을 수 없다는 한계를 protocol.json에 적었다.
- 관측 — **`.env`의 `ANTHROPIC_API_KEY`가 자리표시자로 덮여 있었다.** 수정 시각 17:26이
  Solar 파일럿 시작과 같고 `UPSTAGE_API_KEY`가 추가돼 있어, `.env.example` 복사 과정에서
  실제 키가 사라진 것으로 본다. 두 번 실행해 200건 전부 401, 과금 0. 스크립트에 키가 없으면
  호출 전에 멈추는 가드를 넣었다. 키 복구 후 실행 절차는 `NEXT.md` 0번.
- 산출물: `data/context/document_context_v1.jsonl`, `scripts/evaluation/context_rejudge.py`,
  `reports/current/v5/context_rejudge_100_s42/{protocol.json,context_cards.txt}`.

## 2026-09-08 01:30

- 결정: **문서 맥락 카드는 채택하지 않는다 — 이득이 작고 잡음과 구분되지 않으며, 경계 혼동에서는
  아무것도 바꾸지 않았다.** Anthropic 키가 없어(23:20) Solar로 돌렸다. Solar 파일럿(18:08)의
  같은 100건·같은 프롬프트·같은 설정을 `plain` 팔로 그대로 쓰고, 맥락 카드 한 장을 더한
  `context` 팔만 새로 호출했다(100건 전부 성공, 재시도 포함, 약 $0.03).

  | | plain (파일럿) | context |
  |---|---:|---:|
  | 성공 | 82 | 100 |
  | 정답(v5) 일치 | 49/82 | 62/100 |
  | 고침 / 망침 (앙상블 대비) | 16 / 17 | 21 / 19 |
  | 전체 fold 평균 (100건만 교체) | 0.6714 → 0.6731 | 0.6714 → 0.6714 |

  **공통 성공 82건의 짝 비교**가 결과다. 정답 일치 49 → 54(+5). 두 팔이 다른 답을 낸 15건 중
  plain 틀림→context 맞음 8, 그 반대 3. 부호검정 양측 p = 0.23. 문서별로 보면 이득이
  신용회복위원회(+3)와 대법원(+2)에만 있고 나머지 11개 문서는 0이다.
  **경계 혼동 표본 7건은 plain 3 맞음, context 3 맞음으로 같다.**

- 이유: 맥락을 넣으면 근거에 맥락 단어(폐쇄망·예산·SAP 등)가 9 → 19건으로 늘어 모델이 카드를
  읽기는 했다. 그러나 고친 8건의 근거를 읽으면 대부분 요구사항 원문 안의 단서(IDC 상면료,
  5년 부품 보증, 법령 변경 반영 의무)를 다시 짚은 것이고, 카드의 사실이 판정을 뒤집은 건은
  드물다. 망친 3건은 연계 대상 불특정·유지관리 금액을 새로 blocker/원가로 읽은 것이라
  맥락이 준 "확인된 사실"이 오히려 의심을 늘린 방향이다. 경계 혼동 7건에서 변화가 없으니
  "문서 맥락이 경계를 푼다"는 처방은 이 설계로는 성립하지 않는다.

- 한계: (1) Solar zero-shot이라 v5 라벨러(Claude 층화 퓨샷)와 모델이 다르다. Claude 팔은 키
  복구 후 돌릴 수 있게 스크립트에 남겨 뒀지만 기대 효과가 이 결과보다 클 근거가 없다.
  (2) 경계 혼동이 표본 100건 중 7건뿐이라 경계에 대한 판정력은 약하다. 경계 127건 전수로
  돌려도 건당 $0.0003이라 비용은 문제가 아니지만, 짝 비교 8:3이 p 0.23이라는 것을 보고
  확대할 이유를 찾지 못했다. (3) v5 정답이 맥락 없이 만들어졌다는 순환 문제는 그대로다.
  (4) 카드 13장 중 예산 미기재 4, 지역난방은 요구사항 정의서만이라 카드 자체가 얇은 문서가 있다.

- 관측 — **재시도 백오프만으로 성공률 82 → 100.** 파일럿의 실패 18건(300자 초과 12·토큰
  한도 4·연결 2)은 같은 프롬프트에서 `_sleep_backoff` 6회 재시도로 전부 회수됐다. 파일럿의
  "실패 건은 앙상블 유지" 규칙이 결과를 얼마나 가렸는지 보여준다. 다만 그 18건의 정답 일치는
  8/18로 성공분(54/82)보다 낮아, 실패하는 건이 어려운 건이기도 하다.

- 종합 — 경계 혼동 127건에 대해 이제 네 방향이 닫혔다. 모델 계열·용량·입력(9/2), 결정 구조
  (9/6), 데이터 40% 증가(9/7), **문서 맥락(오늘)**. 처방은 2026-09-07 21:10 이전에 적은 대로
  **2분류 운영 + 경계는 사람 판정**이며, 남은 처방은 결정 21에 blocker·cost_basis 공존 하위
  규칙을 넣어 라벨 정의 자체를 좁히는 것(07:46)뿐이다. 그것은 사람이 할 일이다.

- 논문 메모(보관용):
  - "문서 맥락이 필요하다"는 진단(19:20)과 "문서 맥락 카드를 줘도 안 풀린다"는 처방 실패를
    나란히 놓는다. 진단이 맞아도 그 정보를 카드 한 장으로 요약해 넣는 것으로는 부족하다는
    뜻이고, 필요한 맥락이 무엇인지 사람도 사전에 특정하지 못한다는 것이 더 정확한 서술이다.
  - 실패 건 처리 규칙이 파일럿 결과를 좌우했다(82 대 100). LLM 재판정 실험은 성공률을
    먼저 보고한다.

- 산출물: `scripts/evaluation/context_rejudge.py`(`--provider solar|claude`),
  `data/context/document_context_v1.jsonl`,
  `reports/current/v5/context_rejudge_100_s42_solar/{protocol.json,context_cards.txt,system_prompt_context.txt,results_context.jsonl,summary.json}`.

## 2026-09-08 09:40

- 결정: **경계 혼동 127건을 정성 검토했고, 라벨 규칙 손질(v7)은 기각이 아니라 보류다.** 사용자가
  검토판(C 탭 27건)을 끝내면 결정한다. 검토는 두 겹으로 했다 — Claude가 127건 전부를 v5
  판단 규칙대로 읽어 판정했고, 사용자가 그중 Claude가 "모델이 맞다"고 본 건을 다시 봤다.

- Claude 판정(127건): v5 라벨이 맞다 87(확실 74·기울음 13), 모델 예측이 맞다 27(15·12),
  둘 다 아니고 통상수용 5, 애매 8. **방향이 갈린다.** v5=견적/모델=계약인 62건은 62건 전부
  v5가 맞다 — 서버 사양표·투입 인력·하자보증·표준 보안 조항을 모델이 계약으로 찍은 순수 모델
  오류다. v5=계약/모델=견적인 65건은 v5 25, 모델 27, 통상 5, 애매 8이다.
  `reports/current/v5/boundary127_claude_review.csv`.

- 사용자 판정(14건, 보류 1): Claude와 **같음 8, 다름 5.** 다른 5건이 전부 한 방향이다 —
  대형 GPU 조달(ECR-006), PB급 레이크하우스(SFR-035), 모델 10종 반입 승인(SFR-042), KISA
  120B 중추 모델(SFR-009·010). 사용자 근거: 예산 비중이 크고 가격 변동이 심한 장비, 공급망
  신뢰가 확인된 국산 120B급은 사실상 한 곳, 특정 회사에 맞춰 쓰인 조항. **라벨 기준의
  페르소나(기본 LLM·RAG 역량, 고급 기술자 별도)에서는 "할 수 있는지 먼저 물어야 하는 일"이
  맞고, Claude의 "표준 기능이면 견적"은 전문가 시각이었다.**
  `reports/current/v5/boundary_review_user_judgments.json`.

- 조정: Claude의 "모델이 맞다" 27건에서 GPU·대형 장비 3(ECR-006·INR-DTL-002·AIP-004),
  PB급 1, 모델 반입 1, KISA 5(SFR-008·009·010·012·014)를 v5 쪽으로 되돌려 **17건**으로 본다.
  사용자 일치율(8/13)로 외삽하면 v5가 실제로 틀린 건은 127건 중 10~15건이다.

- 남는 규칙 후보(사용자 판정과도 맞은 것): (1) 수량·개수(10종·20개·100%)는 성능 목표가
  아니다, (2) 임계값·보관 기간·주기 같은 세부 파라미터의 협의는 범위 blocker가 아니다,
  (3) 사양표·인력 요건·하자보증·표준 보안 조항은 계약이 아니라 견적이다(모델 오류 62건의
  자리이기도 하다). 빠진 후보: "표준 장비는 조달 가능" — 대형 GPU·고가 장비에는 안 맞는다.
  사용자가 여러 번 적은 "어렵다 ≠ blocker"는 v5 규칙 그대로이며 판정에서 일관되게 유지됐다.

- 판단: 10~15건이면 재라벨링으로 오르는 점수는 0.01 안팎이라 측정 하한 아래다. 그래서
  **점수 목적의 v7은 근거가 약하다.** 다만 (a) C 탭 27건을 다 보면 비율이 바뀔 수 있고,
  (b) 라벨 정의를 명확히 하는 것은 점수와 별개로 논문 가치가 있으며 비용이 $8이다. 사용자
  검토가 끝난 뒤 다시 판단한다. 점수 레버는 문서 추가와 동일 조건 3회 다수결이 우선이다.

- 도구: 검토판을 Artifact로 올리고 판정을 서버(db)에 저장하게 했다. 로컬 HTML은 폰에서
  못 열고, 복사 붙여넣기는 번거로웠다. 검토판 링크는 `NEXT.md`에 적었다.

- 논문 메모(보관용):
  - 라벨 검토에서 **검토자의 페르소나가 판정을 가른다.** 같은 문장을 전문가는 견적, 기본
    역량 팀은 계약으로 읽는다. v5 라벨이 "과다 판정"으로 보인 상당수는 페르소나를 지키면
    옳다. 라벨 정의에 페르소나를 박아둔 것이 여기서 값을 했다.
  - 모델 오류 62건(견적→계약)과 라벨 논쟁 65건(계약→견적)은 성격이 다르다. 앞은 데이터로
    풀 수 있고, 뒤는 문장 밖 지식(조달 시장·공급망·규모)이라 풀 수 없다.

- 산출물: `reports/current/v5/boundary127_claude_review.csv`,
  `reports/current/v5/boundary_review_user_judgments.json`, Artifact 검토판.

## 2026-09-08 10:40

- 결정: **검토판 C 탭 27건을 사용자가 전부 판정했다. Claude가 "모델이 맞다"고 본 27건 중 사용자도
  모델 편인 것은 15건, v5 편이 11건, 보류 1건.** Claude와의 일치는 15:11. 09:40의 외삽(17건)보다 조금
  낮고, "v5가 실제로 틀린 건은 127건 중 10~15건"이라는 추정은 그대로다.

- 사용자가 v5 편을 든 11건의 근거는 09:40과 같은 축이다. 대형 GPU·HCI 3년 워런티(INR-DTL-002),
  KISA 120B 중추(SFR-012·014), 인간 피드백 강화학습 요구(ASR-006), 소버린 AI 모델 라이선스
  (COR-001·SFR-045). 전부 **조달·공급망·규모**다. 사용자가 모델 편을 든 15건은 GPU 모니터링,
  모델 선택 UI, 플레이그라운드, 모바일 앱, 보안 방안 제시, RAG 기능, 검토 요건 수립처럼
  **"기능 구현이고 어려울 뿐"**인 것들이다. "어렵다 ≠ blocker"가 사용자 판정 27건에서 한 번도
  흔들리지 않았다.

- 관측 — **v5=v6인 라벨도 안정 라벨이 아니다.** 127건 중 v5=v6이 67건인데, 사용자 27건에서
  v5=v6인 7건 중 4건(SER-006·SFR-012·SFR-051·AIP-004)은 "긴 기능 목록을 통째로 계약으로 읽은"
  자리라 두 프롬프트가 같이 틀렸다고 봤다. 반대로 v5≠v6이고 v6이 모델과 같은 8건(GPU 장비·
  PB급·KISA 4건·강화학습·HCI 워런티)은 v6이 blocker를 좁히며 놓친 조달·공급망을 사용자가 다시
  잡은 자리다. 두 프롬프트가 같이 틀리는 자리와 v6이 혼자 틀리는 자리가 따로 있다.

- 판단: 라벨 규칙 v7의 살아남은 후보 셋(수량은 성능 목표 아님, 세부 파라미터 협의는 범위 아님,
  사양표·인력·하자보증은 견적)은 사용자 판정과 맞는다. 하지만 이 규칙이 고칠 라벨은 127건 중
  10~15건이고, 그중 경계 밖 오답까지 넓혀도 재라벨링으로 오르는 macro F1은 0.01 안팎이다.
  **점수 목적으로는 하지 않는다.** 라벨 정의를 명확히 하는 v7은 논문 절(라벨 정의의 페르소나
  의존성)의 근거로 가치가 있으므로, 문서 확충·3회 다수결 뒤에 여력이 있으면 한다.

- 관측 — 사용자 메모에서 반복된 판단 기준을 적어 둔다. 라벨 규칙을 손볼 때 그대로 쓴다.
  (1) GPU·대형 장비는 "가격 변동이 심해 조달 가능성이 불투명"하면 계약. (2) 국산·공급망 검증
  모델처럼 후보가 한둘이면 공급자종속. (3) 라이선스가 붙는 외부 모델(소버린 AI)은 blocker.
  (4) 기능 목록이 길고 정합성이 어려운 것은 견적. (5) "협의"가 세부 파라미터(GPS 대상,
  임계값)면 blocker 아님. (6) 무상 유지보수 1년은 통상, HW 3년 워런티는 판단 유보.

- 산출물: `reports/current/v5/boundary_review_user_judgments.json`(27건, 메모 포함).

## 2026-09-08 11:30

- 결정: **라벨 규칙 v7 재라벨링을 제출했다**(브랜치 `label-v7`). 10:40의 "점수 목적으로는 후순위"를
  사용자가 뒤집었다 — 반복(3회 다수결)은 우연 잡음만 걷어내고 검토에서 본 오류는 체계적이었으므로,
  반복보다 규칙 교정이 먼저라는 판단. 비용은 통상수용을 빼서 절반으로 줄였다.
- 조건: 프롬프트 `system_prompt_v7.txt`(v5 전문 + `[v7 보정]` 5항, sha `b907f3ce…`), 층화 few-shot,
  앵커 풀 **v3**(v2에서 v7 규칙과 충돌하는 9건 제거, 91건), 힌트 없음, Sonnet 5 배치.
  v5와 다른 것은 프롬프트 한 절과 앵커 9건뿐이다.
- 대상: **`통상수용` 제외 695건**(견적 333 + 계약 362). v7 규칙은 판정을 낮추는 방향만 있어
  v5에서 통상수용인 750건은 v7에서도 통상수용이라고 본다(단조성). 그 가정을 통상수용 50건
  (seed 42) 별도 배치로 확인한다. 예상 비용 $6.5.
- 배치 ID: `msgbatch_01WyEgbmyW7CWCYLBstJ3STE`(695), `msgbatch_01NvGJWyMnVWY9br1AnzKzWv`(50).
  결과는 29일 보관되니 회수를 놓치지 않는다(9/6 18:08의 실수 반복 금지).
- 평가 계획: (1) 50건 중 통상수용 유지 비율 — 95% 미만이면 단조성 가정이 틀린 것이고 전수를
  다시 돌려야 한다. (2) 695건에서 v5와 갈린 건의 방향과 수 — 계약 → 견적/통상, 견적 → 통상만
  나와야 한다. 올라간 건이 있으면 규칙이 새는 곳이다. (3) `label_dataset_v7` = v5 통상 750 +
  v7 695로 조립해 동결하고, `word+char` 기준선을 v7으로 학습·평가한 값을 v5의 0.6395와
  나란히 놓는다(각자의 기준). (4) 갈린 건 중 표본을 검토판에 올려 사용자가 본다.
- 산출물: `data/anchors/anchor_pool_v3.{jsonl,_manifest.json}`, `notebooks/prompts/system_prompt_v7.txt`,
  `data/samples/relabel_v7_{nonnormal_695,normal_check_50}.jsonl`,
  `reports/current/claude_batches/relabel_v7_{nonnormal,normal_check}/batch_info.json`.

## 2026-09-08 12:40

- 결정: **`label_dataset_v7`을 동결하되 주 데이터셋으로 채택하지 않는다.** v7으로 학습·평가한
  `word+char` 기준선은 fold 평균 **0.6040**으로 v5의 0.6395보다 **-0.035**다. 측정 하한(0.016)을
  넘는 하락이다. 오답은 401 → 379, 경계 혼동은 127 → 107, 정확도는 0.702 → 0.710으로 나아졌는데
  macro F1은 내려갔다. **v6(03:17)과 같은 패턴이며 폭만 작다.**

  | | v5 | v7 |
  |---|---:|---:|
  | 계약·질의검토 비중 | 25.1% | 19.6% |
  | 통상수용 비중 | 51.9% | 57.5% |
  | Dummy macro F1 | 0.228 | 0.239 |
  | word+char fold 평균 | 0.6395 | **0.6040** |
  | 통합 OOF | 0.654 | 0.644 |
  | 검토 recall | 0.605 | 0.516 |
  | 오답 / 경계 혼동 | 401 / 127 | 379 / 107 |
  | 라벨별 F1 (통상/견적/계약) | 0.811 / 0.532 / 0.618 | 0.831 / 0.547 / 0.554 |

- 배치 결과: 695건 중 692 성공, 3건 실패(JSON 잘림 2, **크레딧 소진 1**). 재시도 배치도 크레딧
  부족으로 제출 실패해 그 3건은 v5 라벨을 유지했다(manifest에 기록). 통상수용 50건 표본은
  49건 유지(98%)로 단조성 가정이 성립한다 — 1건(koen PMR-010, 사무환경·장비 사업자 부담)은
  견적으로 올라갔고 규칙상 옳다.

- 전이(v5 → v7, 692건): 유지 535, 계약→견적 56, 계약→통상 43, 견적→통상 38, **견적→계약 20**.
  올라간 20건은 v7 규칙 5(유지되는 blocker)가 다시 짚은 것들이다 — 특정 벤더 제품명(V3·Red
  Castle 등), RTO 4h/RPO 24h 수치, H100 8ea 조달, 외부 기관(UNI-PASS·MOUS) 연계 승인. 규칙이
  새는 자리가 아니라 v5가 놓쳤던 자리다.
  경계 혼동 127건 중 32건이 바뀌었고 23건은 모델 예측과 같아졌다. 사용자 판정 27건과는
  **18/26 일치**(v5는 정의상 11/26). 라벨은 실무자 판단에 가까워졌다.

- 해석 — **점수와 라벨 품질이 갈라졌다.** 실무자 판정에 더 가까운 라벨셋이 macro F1로는 더 낮다.
  원인은 지표의 구조다. `계약·질의검토`가 25.1% → 19.6%로 얇아지면서 그 클래스의 F1이
  0.618 → 0.554로 내려갔고, macro F1은 세 클래스를 같은 무게로 평균하므로 소수 클래스 하나가
  얇아지면 통상수용이 더 잘 맞아도 상쇄되지 않는다. 오답 수와 정확도는 v7이 낫다.
  v6이 -0.040, 안정 라벨만 학습이 -0.087, v7이 -0.035 — **소수 클래스를 얇게 만드는 조작은
  방향이 옳아도 이 지표에서 손해**라는 것이 세 번째로 확인됐다.

- 판단: 점수가 목표면 v5를 유지한다. 논문에서는 v7을 "라벨 정의를 실무자 판단에 맞춰 좁히면
  점수가 내려간다"의 근거로 쓴다 — 지표가 라벨 분포에 종속된다는 사례이며, 09:40의
  "페르소나 의존성"과 짝이다. 검토판에는 올리지 않는다(사용자 판정 27건으로 방향은 이미 확인).

- 관측 — Anthropic 크레딧이 소진됐다. 3회 다수결이든 문서 확충 라벨링이든 충전 없이는 못 한다.

- 논문 메모(보관용):
  - 라벨 정의를 바꾸면 Dummy 기저선부터 바뀐다(0.228 → 0.239). 데이터셋 간 비교는 각자의
    Dummy 대비로 읽되, macro F1은 소수 클래스 비중에 민감하다는 것을 함께 적는다.
  - 오답 -22, 경계 -20, 정확도 +0.8%p, macro F1 -0.035가 한 실험에서 동시에 나온다. 어느 지표를
    주 지표로 두느냐가 결론을 뒤집는 사례.

- 산출물: `data/labels/label_dataset_v7.{jsonl,_manifest.json}`(sha `a2985e61…`),
  `scripts/labeling/assemble_label_dataset_v7.py`, `DATASET_SPECS["v7"]`,
  `reports/current/claude_batches/relabel_v7_{nonnormal,normal_check}/results.jsonl`,
  `data/samples/relabel_v7_retry_3.jsonl`(미제출).

## 2026-09-08 13:30

- 관측: **v7 라벨은 v5보다 문서를 넘어 더 일관되지만, 그 이득은 TF-IDF가 못 읽는 자리에 있다.**
  19:20의 이웃 분석(E5, 다른 문서 최근접 5건 다수결)을 같은 이웃에 v5·v7 라벨을 각각 얹어 재봤다.

  | 집단 | v5 라벨 일치 | v7 라벨 일치 |
  |---|---:|---:|
  | 전체 1,445 | 64.3% | 66.6% |
  | v5 경계 혼동 127 | 26.0% (19~34) | **39.4%** (31~48) |
  | v7에서 바뀐 157 | 45.2% | 44.6% |
  | 안 바뀐 1,288 | 66.6% | 69.3% |
  | kNN 다수결 macro F1 | 0.598 | 0.602 |

  경계 혼동 자리에서 이웃 일치가 26 → 39%로 올랐다. v7이 그 자리의 라벨을 "비슷한 조항이 다른
  문서에서 받은 라벨" 쪽으로 옮겼다는 뜻이며, 12:40에서 사용자 판정과 18/26 맞은 것과 같은
  방향이다. 그런데 `word+char`는 v7에서 0.604로 v5보다 낮다(12:40). **의미 공간에서는 더
  일관해진 라벨을 표면 어휘 모델은 덜 배운다.** 12:40의 해석(v7의 옳음은 이 모델이 볼 수 없는
  자리에 있다)을 뒷받침한다. 바뀐 157건 자체는 어느 라벨로도 45%라 여전히 흔들리는 자리다.

- 지표 대조(12:40 보충): v5 대 v7이 macro F1 0.640/0.604, weighted F1 0.702/0.701, 정확도
  0.705/0.710, balanced acc 0.645/0.610, Cohen κ 0.507/0.497, 2분류 0.777/0.761, Dummy 대비
  +0.417/+0.365. 정확도만 v7이 낫고 우연 보정 지표는 전부 v5가 낫다.

- 다음: 인코더 파인튜닝을 v7으로 돌려 "의미를 읽는 모델은 v7을 더 잘 배우는가"를 본다
  (gcube, base s7 + large s42, `RFP_DATASET_VERSION=v7`, 브랜치 `label-v7`). 마스킹·트리·군집
  특징은 표면 특징 방법이라 v7에서도 돌리지 않는다.

- 산출물: 없음(기존 캐시·함수 재사용. `cluster_diagnostics.cross_document_neighbors`).

## 2026-09-08 14:00

- 관측: **HDBSCAN은 이 임베딩 공간에서 구조를 찾지 못한다.** E5 임베딩(단위 벡터, 유클리드)에
  `min_cluster_size=5`면 거대 군집 2개 + 잡음 10.5%, `10`이면 군집 12개(중앙값 17건)에 잡음
  72.9%다. 밀도 기반이 잡을 섬이 없고 요구사항이 연속체로 퍼져 있다는 뜻이다(문서 간 코사인이
  0.85~0.95에 몰림). KMeans k=80은 강제로 나누기 때문에 순도 66~69%가 나올 뿐이다.
  - 잡음 오답률은 28~30%로 군집 안(30%)과 같다. 19:20의 "아웃라이어는 오답과 무관"이 밀도
    기준으로도 재현됐고, 임계값을 손으로 정했다는 약점은 이걸로 지운다.
  - 라벨 버전별 순도(mcs=10, 12개 군집): v5 60.5% / v6 57.1% / v7 61.7%. 혼합 군집 행은
    v5 153 / v6 285 / v7 196. v7이 v5와 같거나 조금 낫고 v6이 가장 흐트러진다 — 13:30의
    이웃 일관성과 방향이 같다.
  - 잡음 라벨 분포에서 v6의 `계약·질의검토`가 v5의 절반(125 대 249)이다. v6이 경계를 깎은
    자리가 밀도 낮은 곳(잡음)에도 그대로 있다.
- 판단: 군집 알고리즘을 바꿔 얻을 것은 없다. 군집 축은 18:40의 "임베딩 축 닫음"에 포함해 닫는다.
- 산출물: 없음(탐색).
